{
  "name": "crystal-relay-live-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Temporary live feedback heartbeat list for Crystal Relay.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
