<Window x:Class="VrcTwitchOscBridge.VrChatLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:VrcTwitchOscBridge.Services"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{loc:Translate 'VRChat Login | Crystal Relay'}"
        Icon="Assets/crystal-relay-icon.ico"
        Width="500"
        MinHeight="430"
        MinWidth="500"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource BodyFontFamily}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{DynamicResource WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <FontFamily x:Key="BodyFontFamily">Verdana</FontFamily>
        <FontFamily x:Key="HeadingFontFamily">Constantia</FontFamily>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#130B1E" />
        <SolidColorBrush x:Key="PanelBrush" Color="#CC1C132B" />
        <SolidColorBrush x:Key="BorderBrush" Color="#4B2B78" />
        <SolidColorBrush x:Key="AccentBrush" Color="#A855F7" />
        <SolidColorBrush x:Key="TextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="MutedBrush" Color="#C9B8E3" />
        <SolidColorBrush x:Key="InputBrush" Color="#B8271A3D" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#5B3A8E" />
        <SolidColorBrush x:Key="ComboSurfaceBrush" Color="#F7F2FF" />
        <SolidColorBrush x:Key="ComboTextBrush" Color="#140C20" />
        <SolidColorBrush x:Key="ComboHighlightBrush" Color="#D8C2FF" />
        <SolidColorBrush x:Key="StatusChipBrush" Color="#8E241841" />
        <SolidColorBrush x:Key="ErrorBrush" Color="#FF6B6B" />
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#2C1C48" />
        <SolidColorBrush x:Key="SecondaryButtonBorderBrush" Color="#6942A7" />
        <SolidColorBrush x:Key="TitleBarBrush" Color="#20122F" />
        <SolidColorBrush x:Key="TitleBarTextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="TitleBarSubTextBrush" Color="#CBB9E5" />
        <SolidColorBrush x:Key="TitleBarButtonBrush" Color="#00000000" />
        <SolidColorBrush x:Key="TitleBarButtonHoverBrush" Color="#3B235B" />
        <SolidColorBrush x:Key="TitleBarCloseHoverBrush" Color="#B43D62" />

        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{DynamicResource TitleBarButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Surface" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Surface" Property="Background" Value="{DynamicResource TitleBarButtonHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TitleBarCloseHoverBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="HeadingTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource HeadingFontFamily}" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,8,0,0" />
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="Margin" Value="0,8,0,0" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ComboSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ComboTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="Margin" Value="0,8,0,0" />
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="{DynamicResource HeadingFontFamily}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryButtonBorderBrush}" />
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            Background="{DynamicResource WindowBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="46" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Background="{DynamicResource TitleBarBrush}"
                  MouseLeftButtonDown="OnTitleBarMouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Margin="16,0,0,0"
                            VerticalAlignment="Center">
                    <TextBlock Text="{loc:Translate 'VRChat Login'}"
                               Style="{StaticResource HeadingTextStyle}"
                               FontSize="18"
                               FontWeight="Bold" />
                    <TextBlock Text="{loc:Translate 'Enter your VRChat credentials to load avatar choices.'}"
                               Foreground="{DynamicResource TitleBarSubTextBrush}"
                               FontSize="11" />
                </StackPanel>
                <Button Grid.Column="1"
                        Style="{StaticResource CloseButtonStyle}"
                        Content="{loc:Translate 'Close'}"
                        Click="OnCloseClicked" />
            </Grid>

            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <Grid Margin="22">
                    <Border Background="{DynamicResource PanelBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="22"
                            Padding="24">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Text="{loc:Translate 'VRChat Avatar Access'}"
                                       Style="{StaticResource HeadingTextStyle}"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="24"
                                       FontWeight="Bold" />

                            <TextBlock Grid.Row="1"
                                       Margin="0,10,0,0"
                                       Text="{loc:Translate 'Crystal Relay only uses this login to validate your VRChat session and load avatar choices for avatar-change rules.'}"
                                       Foreground="{DynamicResource MutedBrush}"
                                       TextWrapping="Wrap" />

                            <Grid Grid.Row="2"
                                  Margin="0,20,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{loc:Translate 'Username or Email'}"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="SemiBold" />

                                <TextBox x:Name="UsernameTextBox"
                                         Grid.Row="1" />

                                <TextBlock Grid.Row="2"
                                           Margin="0,16,0,0"
                                           Text="{loc:Translate 'Password'}"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="SemiBold" />

                                <PasswordBox x:Name="PasswordInput"
                                             Grid.Row="3" />
                            </Grid>

                            <Border Grid.Row="3"
                                    Margin="0,18,0,0"
                                    Padding="12,10"
                                    Background="{DynamicResource StatusChipBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="14">
                                <TextBlock Text="{loc:Translate 'Your password is used only for this login step and is not saved by Crystal Relay.'}"
                                           Foreground="{DynamicResource MutedBrush}"
                                           TextWrapping="Wrap" />
                            </Border>

                            <TextBlock x:Name="StatusTextBlock"
                                       Grid.Row="4"
                                       Margin="0,14,0,0"
                                       Foreground="{DynamicResource ErrorBrush}"
                                       TextWrapping="Wrap"
                                       Visibility="Collapsed" />

                            <Grid x:Name="TwoFactorSection"
                                  Grid.Row="5"
                                  Margin="0,14,0,0"
                                  Visibility="Collapsed">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Border Background="{DynamicResource StatusChipBrush}"
                                        BorderBrush="{DynamicResource AccentBrush}"
                                        BorderThickness="1"
                                        CornerRadius="14"
                                        Padding="12,10">
                                    <TextBlock x:Name="TwoFactorTitleText"
                                               Text="{loc:Translate 'Two-factor authentication is required for this account. Enter the verification code below.'}"
                                               Foreground="{DynamicResource MutedBrush}"
                                               TextWrapping="Wrap" />
                                </Border>

                                <TextBlock Grid.Row="1"
                                           Margin="0,16,0,0"
                                           Text="{loc:Translate 'Verification Method'}"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="SemiBold" />

                                <ComboBox x:Name="MethodComboBox"
                                          Grid.Row="2"
                                          SelectionChanged="OnMethodSelectionChanged" />

                                <TextBlock Grid.Row="3"
                                           Margin="0,16,0,0"
                                           Text="{loc:Translate 'Verification Code'}"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="SemiBold" />

                                <TextBox x:Name="CodeTextBox"
                                         Grid.Row="4"
                                         MaxLength="12"
                                         CharacterCasing="Upper"
                                         IsEnabled="True" />

                                <TextBlock x:Name="HintTextBlock"
                                           Grid.Row="5"
                                           Margin="0,8,0,0"
                                           Foreground="{DynamicResource MutedBrush}"
                                           TextWrapping="Wrap"
                                           FontSize="12" />
                            </Grid>

                            <StackPanel Grid.Row="6"
                                        Margin="0,20,0,0"
                                        Orientation="Horizontal"
                                        HorizontalAlignment="Right">
                                <Button x:Name="CancelButton"
                                        Content="{loc:Translate 'Cancel'}"
                                        Width="98"
                                        Margin="0,0,10,0"
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        Click="OnCancelClicked" />
                                <Button x:Name="ContinueButton"
                                        Content="{loc:Translate 'Continue'}"
                                        Width="116"
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        IsDefault="True"
                                        Click="OnContinueClicked" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>