<Window x:Class="VrcTwitchOscBridge.VrChatTwoFactorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:VrcTwitchOscBridge"
        xmlns:loc="clr-namespace:VrcTwitchOscBridge.Services"
        Title="{loc:Translate 'VRChat 2FA | Crystal Relay'}"
        Icon="Assets/crystal-relay-icon.ico"
        Width="510"
        MinHeight="485"
        MinWidth="510"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource BodyFontFamily}"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Window.Resources>
        <FontFamily x:Key="BodyFontFamily">Verdana</FontFamily>
        <FontFamily x:Key="HeadingFontFamily">Constantia</FontFamily>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#130B1E" />
        <SolidColorBrush x:Key="PanelBrush" Color="#CC1C132B" />
        <SolidColorBrush x:Key="BorderBrush" Color="#4B2B78" />
        <SolidColorBrush x:Key="AccentBrush" Color="#A855F7" />
        <SolidColorBrush x:Key="TextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="MutedBrush" Color="#C9B8E3" />
        <SolidColorBrush x:Key="InputBrush" Color="#B8271A3D" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#5B3A8E" />
        <SolidColorBrush x:Key="ComboSurfaceBrush" Color="#F7F2FF" />
        <SolidColorBrush x:Key="ComboTextBrush" Color="#140C20" />
        <SolidColorBrush x:Key="ComboHighlightBrush" Color="#D8C2FF" />
        <SolidColorBrush x:Key="StatusChipBrush" Color="#8E241841" />
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#2C1C48" />
        <SolidColorBrush x:Key="SecondaryButtonBorderBrush" Color="#6942A7" />

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="HeadingTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource HeadingFontFamily}" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,10" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,8,0,0" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ComboSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ComboTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="MinHeight" Value="44" />
            <Setter Property="Margin" Value="0,8,0,0" />
            <Setter Property="{x:Static local:ComboBoxInteraction.EnableGuardedInteractionProperty}" Value="True" />
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource ComboSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource ComboTextBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboHighlightBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ComboHighlightBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource ComboTextBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="{DynamicResource HeadingFontFamily}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="14"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryButtonBorderBrush}" />
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="22">
            <Border Background="{DynamicResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="22"
                    Padding="24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="{loc:Translate 'VRChat 2-Factor Check'}"
                               Style="{StaticResource HeadingTextStyle}"
                               Foreground="{DynamicResource TextBrush}"
                               FontSize="24"
                               FontWeight="Bold" />

                    <TextBlock Grid.Row="1"
                               Margin="0,10,0,0"
                               Text="{loc:Translate 'Enter the code VRChat is asking for so Crystal Relay can finish loading your avatar list.'}"
                               Foreground="{DynamicResource MutedBrush}"
                               TextWrapping="Wrap" />

                    <Grid Grid.Row="2"
                          Margin="0,20,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Text="{loc:Translate 'Code Type'}"
                                   Foreground="{DynamicResource TextBrush}"
                                   FontWeight="SemiBold" />

                        <ComboBox x:Name="MethodComboBox"
                                  Grid.Row="1"
                                  SelectionChanged="OnMethodSelectionChanged" />

                        <TextBlock Grid.Row="2"
                                   Margin="0,16,0,0"
                                   Text="{loc:Translate 'Verification Code'}"
                                   Foreground="{DynamicResource TextBrush}"
                                   FontWeight="SemiBold" />

                        <TextBox x:Name="CodeTextBox"
                                 Grid.Row="3"
                                 MaxLength="64" />
                    </Grid>

                    <Border Grid.Row="4"
                            Margin="0,18,0,0"
                            Padding="12,10"
                            Background="{DynamicResource StatusChipBrush}"
                            BorderBrush="{DynamicResource InputBorderBrush}"
                            BorderThickness="1"
                            CornerRadius="14">
                        <TextBlock x:Name="HintTextBlock"
                                   Foreground="{DynamicResource MutedBrush}"
                                   TextWrapping="Wrap" />
                    </Border>

                    <StackPanel Grid.Row="5"
                                Margin="0,20,0,0"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Content="{loc:Translate 'Cancel'}"
                                Width="98"
                                Margin="0,0,10,0"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="OnCancelClicked" />
                        <Button Content="{loc:Translate 'Verify'}"
                                Width="116"
                                Style="{StaticResource PrimaryButtonStyle}"
                                IsDefault="True"
                                Click="OnVerifyClicked" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</Window>
