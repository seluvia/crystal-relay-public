<UserControl x:Class="VrcTwitchOscBridge.UserControls.RoulettePoolEditorControl"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
          xmlns:loc="clr-namespace:VrcTwitchOscBridge.Services"
          mc:Ignorable="d" 
          d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="RowIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="26" />
            <Setter Property="Height" Value="26" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SecondaryButtonBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Pool Header -->
        <Border Grid.Row="0" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource InputBorderBrush}" 
                  BorderThickness="1" Padding="8,6" CornerRadius="4" Margin="0,0,0,4">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Content="➕" Width="26" Height="26" Margin="0,0,2,0"
                            ToolTip="{loc:Translate 'Add Avatar'}" 
                            Command="{Binding AddAvatarCommand}"
                            Style="{StaticResource RowIconButtonStyle}" />
                </StackPanel>
                <TextBlock Text="{loc:Translate 'Avatar Pool'}" FontWeight="Bold" 
                             VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" />
            </DockPanel>
        </Border>

        <!-- Avatar Pool List -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Pool}" Margin="0,0,0,4">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource InputBorderBrush}" 
                            BorderThickness="1" Padding="8,6" CornerRadius="4" Margin="0,0,0,4">
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="🗑" Width="26" Height="26" 
                                         ToolTip="{loc:Translate 'Remove Avatar'}"
                                         Command="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=DataContext.RemoveAvatarCommand}"
                                         CommandParameter="{Binding}" 
                                         Style="{StaticResource RowIconButtonStyle}" Margin="0,0,2,0"/>
                            </StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="{Binding ThumbnailUrl, TargetNullValue={StaticResource DefaultAvatarThumbnail}}" 
                                       Width="48" Height="48" Margin="0,0,8,0" 
                                       VerticalAlignment="Top" HorizontalAlignment="Left"
                                       Stretch="UniformToFill"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding AvatarName}" FontWeight="Bold" 
                                              Foreground="{DynamicResource TextBrush}" />
                                    <TextBlock Text="{Binding AvatarId}" FontSize="10" 
                                                Foreground="{DynamicResource MutedBrush}" />
                                </StackPanel>
                            </Grid>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Pool Actions -->
        <Border Grid.Row="2" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource InputBorderBrush}" 
                  BorderThickness="1" Padding="8,6" CornerRadius="4">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{loc:Translate 'Add From Avatar Library'}" Command="{Binding AddFromLibraryCommand}"
                        Style="{DynamicResource PrimaryButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="{loc:Translate 'Clear Pool'}" 
                         Command="{Binding ClearPoolCommand}" 
                         Style="{DynamicResource SecondaryButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>