<UserControl x:Class="VrcTwitchOscBridge.UserControls.InlineAvatarSwapRuleRowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:VrcTwitchOscBridge.Services"
             xmlns:vm="clr-namespace:VrcTwitchOscBridge.ViewModels"
             xmlns:models="clr-namespace:VrcTwitchOscBridge.Models">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Margin" Value="0,0,0,2" />
        </Style>
        <Style TargetType="TextBox" x:Key="InputStyle">
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Padding" Value="4,2" />
            <Setter Property="FontSize" Value="11" />
        </Style>
        <Style TargetType="Button" x:Key="InlineButtonStyle" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="8,2" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </UserControl.Resources>
    <StackPanel>
        <Border Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource InputBorderBrush}" BorderThickness="1" Padding="6,4" CornerRadius="4" Margin="0,0,0,2">
            <DockPanel>
                <TextBlock Text="{Binding Summary}" FontSize="11" VerticalAlignment="Center" />
                <Button DockPanel.Dock="Right" Content="🗑" Command="{Binding DataContext.DeleteRuleCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Width="22" Background="Transparent" Foreground="{DynamicResource MutedBrush}" BorderThickness="0" />
            </DockPanel>
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.BeginInlineEditCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" />
            </Border.InputBindings>
        </Border>
        <Border Background="{DynamicResource NestedPanelBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="5" Padding="10,8"
                Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVis}}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{loc:Translate 'Edit Trigger'}" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,6" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="{loc:Translate 'Done'}" Command="{Binding DataContext.CommitInlineEditCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource InlineButtonStyle}" Background="{DynamicResource AccentBrush}" Foreground="{DynamicResource ComboTextBrush}" BorderBrush="{DynamicResource AccentBrush}" Margin="0,0,4,0" />
                        <Button Content="{loc:Translate 'Cancel'}" Command="{Binding DataContext.CancelInlineEditCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{StaticResource InlineButtonStyle}" Background="{DynamicResource SecondaryButtonBrush}" Foreground="{DynamicResource TextBrush}" BorderBrush="{DynamicResource SecondaryButtonBorderBrush}" />
                    </StackPanel>
                </Grid>

                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Name'}" />
                <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Twitch Reward'}" />
                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Reward Name'}" />
                <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.ChannelPointRewardTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Reward Description'}" />
                <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.ChannelPointRewardDescription, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0,0,0,6" />

                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Delete Reward When Inactive'}" />
                <CheckBox IsChecked="{Binding Rule.DeleteManagedRewardWhenInactive, UpdateSourceTrigger=PropertyChanged}" Content="{loc:Translate 'Free Twitch reward slots when inactive'}" Foreground="{DynamicResource MutedBrush}" Margin="0,0,0,6" />

                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Active Time (seconds)'}" />
                <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.DurationSeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Cooldown (seconds)'}" />
                <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.CooldownSeconds, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

                <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Bot Reply'}" />
                <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.BotMessageTemplate, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="60" TextWrapping="Wrap" Margin="0,0,0,6" />

                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Rule.TriggerType}" Value="{x:Static models:TwitchTriggerType.Bits}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Rule.TriggerType}" Value="{x:Static models:TwitchTriggerType.Subscriptions}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Rule.TriggerType}" Value="{x:Static models:TwitchTriggerType.GiftSubscription}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Rule.TriggerType}" Value="{x:Static models:TwitchTriggerType.PowerUp}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Minimum Amount'}" />
                    <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.MinimumAmount, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                </StackPanel>

                <StackPanel>
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Rule.TriggerType}" Value="{x:Static models:TwitchTriggerType.ChatCommand}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                    <TextBlock Style="{StaticResource LabelStyle}" Text="{loc:Translate 'Chat Command'}" />
                    <TextBox Style="{StaticResource InputStyle}" Text="{Binding Rule.ChatCommandText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />
                </StackPanel>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
