<Window x:Class="VrcTwitchOscBridge.AvatarScalingManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:VrcTwitchOscBridge.Services"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:models="clr-namespace:VrcTwitchOscBridge.Models"
        xmlns:vm="clr-namespace:VrcTwitchOscBridge.ViewModels"
        xmlns:local="clr-namespace:VrcTwitchOscBridge"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:AvatarScalingManagerViewModel}"
        Title="{loc:Translate 'Avatar Scaling Manager'}"
        Icon="Assets/crystal-relay-icon.ico"
        Width="1100"
        Height="700"
        MinWidth="860"
        MinHeight="540"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource BodyFontFamily}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{DynamicResource WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#130B1E" />
        <SolidColorBrush x:Key="PanelBrush" Color="#CC1C132B" />
        <SolidColorBrush x:Key="PanelSecondaryBrush" Color="#C8241739" />
        <SolidColorBrush x:Key="NestedPanelBrush" Color="#B8241739" />
        <SolidColorBrush x:Key="BorderBrush" Color="#4B2B78" />
        <SolidColorBrush x:Key="AccentBrush" Color="#A855F7" />
        <SolidColorBrush x:Key="AccentDimBrush" Color="#552D1B47" />
        <SolidColorBrush x:Key="TextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="MutedBrush" Color="#C9B8E3" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#5B3A8E" />
        <SolidColorBrush x:Key="InputBrush" Color="#241733" />
        <SolidColorBrush x:Key="ComboTextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="ComboSurfaceBrush" Color="#241733" />
        <SolidColorBrush x:Key="ComboHighlightBrush" Color="#4B2B78" />
        <SolidColorBrush x:Key="ComboDropButtonBrush" Color="#2C1C48" />
        <SolidColorBrush x:Key="ComboDropButtonHoverBrush" Color="#3B235B" />
        <SolidColorBrush x:Key="ComboDropButtonPressedBrush" Color="#543183" />
        <SolidColorBrush x:Key="PopupBorderBrush" Color="#8B5CD6" />
        <SolidColorBrush x:Key="RuleCardHoverBrush" Color="#7C3AED" />
        <SolidColorBrush x:Key="ScrollTrackBrush" Color="#241733" />
        <SolidColorBrush x:Key="ScrollThumbBrush" Color="#7C3AED" />
        <SolidColorBrush x:Key="ScrollThumbHoverBrush" Color="#A855F7" />
        <SolidColorBrush x:Key="ScrollThumbPressedBrush" Color="#C084FC" />
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#2C1C48" />
        <SolidColorBrush x:Key="SecondaryButtonBorderBrush" Color="#6942A7" />
        <SolidColorBrush x:Key="TitleBarBrush" Color="#20122F" />
        <SolidColorBrush x:Key="TitleBarTextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="TitleBarSubTextBrush" Color="#CBB9E5" />
        <SolidColorBrush x:Key="TitleBarButtonBrush" Color="Transparent" />
        <SolidColorBrush x:Key="TitleBarButtonHoverBrush" Color="#3B235B" />
        <SolidColorBrush x:Key="TitleBarButtonPressedBrush" Color="#543183" />
        <SolidColorBrush x:Key="TitleBarCloseHoverBrush" Color="#B43D62" />
        <SolidColorBrush x:Key="TitleBarClosePressedBrush" Color="#8C2648" />
        <FontFamily x:Key="HeadingFontFamily">Constantia</FontFamily>
        <FontFamily x:Key="BodyFontFamily">Verdana</FontFamily>

        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <local:EnumBooleanConverter x:Key="EnumBooleanConverter" />

        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbChrome"
                                Margin="2"
                                Background="{DynamicResource ScrollThumbBrush}"
                                CornerRadius="8" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource ScrollThumbHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource ScrollThumbPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarTrackButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Width="{TemplateBinding Width}"
                  MinWidth="18"
                  Background="Transparent">
                <Border Background="{DynamicResource ScrollTrackBrush}"
                        BorderBrush="{DynamicResource InputBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8" />
                <Track x:Name="PART_Track"
                       Margin="2"
                       IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageUpCommand}" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageDownCommand}" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Height="{TemplateBinding Height}"
                  MinHeight="18"
                  Background="Transparent">
                <Border Background="{DynamicResource ScrollTrackBrush}"
                        BorderBrush="{DynamicResource InputBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8" />
                <Track x:Name="PART_Track"
                       Margin="2">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageLeftCommand}" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumbStyle}"
                               MinWidth="32" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageRightCommand}" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="{DynamicResource ScrollTrackBrush}" />
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="18" />
                    <Setter Property="Template" Value="{StaticResource HorizontalScrollBarTemplate}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ScrollViewer">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    Grid.Row="0"
                                                    Grid.Column="0"
                                                    Margin="{TemplateBinding Padding}"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    Content="{TemplateBinding Content}" />
                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Row="0"
                                       Grid.Column="1"
                                       Margin="10,0,0,0"
                                       VerticalAlignment="Stretch"
                                       Orientation="Vertical"
                                       Minimum="0"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Row="1"
                                       Grid.Column="0"
                                       Margin="0,10,0,0"
                                       HorizontalAlignment="Stretch"
                                       Orientation="Horizontal"
                                       Minimum="0"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Value="{Binding HorizontalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="{DynamicResource TitleBarTextBrush}" />
        </Style>

        <Style x:Key="ComboBoxToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="{DynamicResource ComboDropButtonBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="DropButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0,8,8,0">
                            <Path x:Name="DropArrow"
                                  Width="11"
                                  Height="7"
                                  Stretch="Fill"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 0 0 L 5.5 6 L 11 0"
                                  Stroke="{DynamicResource TitleBarTextBrush}"
                                  StrokeThickness="1.8"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DropButtonBorder" Property="Background" Value="{DynamicResource ComboDropButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="DropButtonBorder" Property="Background" Value="{DynamicResource ComboDropButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="DropButtonBorder" Property="Background" Value="{DynamicResource ComboDropButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="DropButtonBorder" Property="Opacity" Value="0.55" />
                                <Setter TargetName="DropArrow" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboPopupTrackButtonStyle" TargetType="RepeatButton">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboPopupThumbStyle" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ComboPopupThumbBorder"
                                Width="8"
                                HorizontalAlignment="Center"
                                Background="{DynamicResource ScrollThumbBrush}"
                                CornerRadius="4" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ComboPopupThumbBorder" Property="Background" Value="{DynamicResource ScrollThumbHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ComboPopupThumbBorder" Property="Background" Value="{DynamicResource ScrollThumbPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboPopupVerticalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Width="{TemplateBinding Width}" Background="Transparent">
                <Border Width="8"
                        HorizontalAlignment="Center"
                        Margin="0,2"
                        Background="{DynamicResource ScrollTrackBrush}"
                        BorderBrush="{DynamicResource InputBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4" />
                <Track x:Name="PART_Track"
                       Margin="0,2"
                       IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ComboPopupTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageUpCommand}" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ComboPopupThumbStyle}" MinHeight="30" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ComboPopupTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageDownCommand}" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="ComboPopupHorizontalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Height="{TemplateBinding Height}" Background="Transparent">
                <Border Height="8"
                        VerticalAlignment="Center"
                        Margin="2,0"
                        Background="{DynamicResource ScrollTrackBrush}"
                        BorderBrush="{DynamicResource InputBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4" />
                <Track x:Name="PART_Track" Margin="2,0">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ComboPopupTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageLeftCommand}" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ComboPopupThumbStyle}" MinWidth="30" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ComboPopupTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageRightCommand}" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style x:Key="ComboPopupScrollBarStyle" TargetType="ScrollBar">
            <Setter Property="Width" Value="12" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template" Value="{StaticResource ComboPopupVerticalScrollBarTemplate}" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto" />
                    <Setter Property="Height" Value="12" />
                    <Setter Property="Template" Value="{StaticResource ComboPopupHorizontalScrollBarTemplate}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="ComboBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
                                        </Style>
                                    </Grid.Resources>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="30" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="ContentSite"
                                                      Grid.Column="0"
                                                      Margin="10,0,4,0"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                      TextElement.Foreground="{DynamicResource TitleBarTextBrush}"
                                                      IsHitTestVisible="False" />
                                    <TextBox x:Name="PART_EditableTextBox"
                                             Grid.Column="0"
                                             Margin="10,0,4,0"
                                             Foreground="{DynamicResource TitleBarTextBrush}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Visibility="Hidden"
                                             IsReadOnly="{TemplateBinding IsReadOnly}" />
                                    <ToggleButton Grid.Column="1"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Style="{StaticResource ComboBoxToggleButtonStyle}" />
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Margin="0,4,0,0"
                                            Padding="4"
                                            Background="{DynamicResource InputBrush}"
                                            TextElement.Foreground="{DynamicResource TitleBarTextBrush}"
                                            BorderBrush="{DynamicResource PopupBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="10">
                                        <ScrollViewer SnapsToDevicePixels="True">
                                            <ScrollViewer.Resources>
                                                <Style TargetType="ScrollBar" BasedOn="{StaticResource ComboPopupScrollBarStyle}" />
                                            </ScrollViewer.Resources>
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ComboBorder" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Margin="0,1"
                                Padding="{TemplateBinding Padding}"
                                Background="Transparent"
                                CornerRadius="6">
                            <Border.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
                                </Style>
                            </Border.Resources>
                            <ContentPresenter TextElement.Foreground="{DynamicResource TitleBarTextBrush}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboHighlightBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboHighlightBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.Resources>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextWrapping" Value="Wrap" />
                                    <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
                                </Style>
                            </Grid.Resources>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="20"
                                    Height="20"
                                    VerticalAlignment="Center"
                                    Background="Transparent">
                                <Grid>
                                    <Border x:Name="CheckChrome"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="7" />
                                    <Path x:Name="CheckMark"
                                          Width="11"
                                          Height="8"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 1 5 L 4.5 8 L 11 1"
                                          Stroke="White"
                                          StrokeThickness="2.2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Visibility="Collapsed" />
                                </Grid>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                              Margin="10,0,0,0"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              ContentSource="Content" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckChrome" Property="BorderBrush" Value="{DynamicResource RuleCardHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckChrome" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="CheckChrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckChrome" Property="Opacity" Value="0.45" />
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource AccentDimBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>

        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Background" Value="{DynamicResource TitleBarButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="TitleBarButtonBorder" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TitleBarButtonBorder" Property="Background" Value="{DynamicResource TitleBarButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="TitleBarButtonBorder" Property="Background" Value="{DynamicResource TitleBarButtonPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TitleBarCloseHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TitleBarClosePressedBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="NavRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PanelSecondaryBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="NavBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardGroupStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource NestedPanelBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="16" />
            <Setter Property="Padding" Value="14" />
            <Setter Property="Margin" Value="0,0,0,12" />
        </Style>

        <DataTemplate x:Key="SourceCardTemplate" DataType="{x:Type vm:AvatarScalingSourceCardViewModel}">
            <Border Margin="0,0,0,8"
                    Padding="10"
                    CornerRadius="12"
                    Background="{DynamicResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1">
                <StackPanel>
                    <DockPanel LastChildFill="True">
                        <Border DockPanel.Dock="Right"
                                Background="{DynamicResource AccentDimBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="10"
                                Padding="6,2"
                                Margin="6,0,0,0">
                            <TextBlock Text="{Binding SourcePill}" FontSize="9" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                        </Border>
                        <StackPanel MinWidth="0">
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="13" Foreground="{DynamicResource TitleBarTextBrush}" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource TitleBarSubTextBrush}" FontSize="10" TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                    </DockPanel>
                    <TextBlock Margin="0,6,0,0" Text="{Binding ActionSummary}" Foreground="{DynamicResource TitleBarSubTextBrush}" FontSize="10" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" />
                    <TextBlock Margin="0,2,0,0" Text="{Binding SafetySummary}" Foreground="{DynamicResource TitleBarSubTextBrush}" FontSize="10" TextTrimming="CharacterEllipsis" />
                    <DockPanel Margin="0,8,0,0" LastChildFill="False">
                        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                            <Button Content="{loc:Translate 'Edit'}"
                                    Command="{Binding DataContext.OpenEditorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                            <Button Margin="8,0,0,0"
                                    Content="{loc:Translate 'Delete'}"
                                    Command="{Binding DataContext.DeleteCardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Kind}" Value="MasterReward">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                        <Button DockPanel.Dock="Right"
                                Content="{loc:Translate 'Avatar Scaling Card Test'}"
                                Command="{Binding DataContext.TestCardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Visibility="{Binding CanTest, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </DockPanel>
                </StackPanel>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource WindowBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource TitleBarBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="OnTitleBarMouseDown">
                <DockPanel LastChildFill="True">
                    <Button DockPanel.Dock="Right"
                            Style="{StaticResource TitleBarCloseButtonStyle}"
                            Click="OnCloseClicked"
                            AutomationProperties.Name="{loc:Translate 'Close'}"
                            ToolTip="{loc:Translate 'Close'}"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button.Content>
                            <Path Width="10" Height="10" Stretch="Uniform" Stroke="{DynamicResource TitleBarTextBrush}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 0,0 L 10,10 M 10,0 L 0,10" />
                        </Button.Content>
                    </Button>
                    <StackPanel Margin="14,9,14,9">
                        <TextBlock Text="{loc:Translate 'Avatar Scaling Manager'}"
                                   FontFamily="{DynamicResource HeadingFontFamily}"
                                   FontWeight="Bold"
                                   FontSize="16"
                                   Foreground="{DynamicResource TitleBarTextBrush}" />
                        <TextBlock Text="{loc:Translate 'Manage Twitch reward scaling, Supporter Growth, Cash Payment scaling, and Power Up scaling.'}"
                                   Foreground="{DynamicResource TitleBarSubTextBrush}"
                                   FontSize="11" />
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Row="1" Margin="14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="190" />
                    <ColumnDefinition Width="*" MinWidth="280" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="370" MinWidth="300" MaxWidth="600" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                        Background="{DynamicResource NestedPanelBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="16"
                        Padding="12"
                        Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="{loc:Translate 'Scaling Sources'}" FontWeight="Bold" FontSize="14" />
                        <TextBlock Margin="0,4,0,12"
                                   Text="{loc:Translate 'Choose which Avatar Scaling source type to review.'}"
                                    Foreground="{DynamicResource TitleBarSubTextBrush}" />

                        <RadioButton Content="{loc:Translate 'All Sources'}"
                                     GroupName="AvatarScalingSources"
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     IsChecked="{Binding ActiveSourceView, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=AllSources, Mode=TwoWay}" />
                        <RadioButton Content="{loc:Translate 'Twitch Rewards'}"
                                     GroupName="AvatarScalingSources"
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     IsChecked="{Binding ActiveSourceView, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=TwitchRewards, Mode=TwoWay}" />
                        <RadioButton Content="{loc:Translate 'Supporter Growth'}"
                                     GroupName="AvatarScalingSources"
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     IsChecked="{Binding ActiveSourceView, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=SupporterGrowth, Mode=TwoWay}" />
                        <RadioButton Content="{loc:Translate 'Cash Payments'}"
                                     GroupName="AvatarScalingSources"
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     IsChecked="{Binding ActiveSourceView, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=CashPayments, Mode=TwoWay}" />
                        <RadioButton Content="{loc:Translate 'Power Ups'}"
                                     GroupName="AvatarScalingSources"
                                     Style="{StaticResource NavRadioButtonStyle}"
                                     IsChecked="{Binding ActiveSourceView, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=PowerUps, Mode=TwoWay}" />
                    </StackPanel>
                </Border>

                <GridSplitter Grid.Column="2"
                              Width="5"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              Background="Transparent"
                              ResizeBehavior="PreviousAndNext"
                              ShowsPreview="True"
                              ResizeDirection="Columns" />

                <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <Border Background="{DynamicResource NestedPanelBrush}"
                                BorderBrush="{DynamicResource AccentBrush}"
                                 BorderThickness="1"
                                 CornerRadius="16"
                                 Padding="14"
                                 Margin="0,0,0,12">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{loc:Translate 'Global Safety Rule'}" FontWeight="Bold" FontSize="14" />
                                        <TextBlock Margin="0,4,0,0" Text="{loc:Translate 'Current Max Height Allowed'}" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                        <TextBlock Text="{Binding CurrentMaxHeightAllowedText}" FontWeight="Bold" FontSize="24" />
                                    </StackPanel>
                                    <Button Grid.Column="1"
                                            Content="{loc:Translate 'Open Advanced Safety'}"
                                            Command="{Binding OpenAdvancedSafetyCommand}"
                                            Style="{StaticResource AccentButtonStyle}"
                                            Margin="12,0,0,0"
                                            VerticalAlignment="Top" />
                                </Grid>

                                <WrapPanel Margin="0,12,0,0">
                                    <Button Content="{loc:Translate 'Add Scale Set'}"
                                            Command="{Binding AddAvatarScaleSetCommand}"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                    <Button Content="{loc:Translate 'Add Scale Redeem'}"
                                            Command="{Binding AddAvatarScaleRuleCommand}"
                                            Style="{StaticResource SecondaryButtonStyle}" />
                                </WrapPanel>

                                <Border Margin="0,10,0,0"
                                        Padding="10"
                                        CornerRadius="12"
                                        Background="{DynamicResource PanelSecondaryBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        Visibility="{Binding IsAdvancedSafetyOpen, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Translate 'Safety &amp; Pairing'}" FontWeight="SemiBold" />
                                        <UniformGrid Columns="2">
                                            <StackPanel Margin="0,0,6,0">
                                                <TextBlock Text="{loc:Translate 'Minimum Height'}" FontWeight="SemiBold" />
                                                <TextBox Text="{Binding Settings.AvatarScaleSafety.CurrentMinimumHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                            </StackPanel>
                                            <StackPanel Margin="6,0,0,0">
                                                <TextBlock Text="{loc:Translate 'Maximum Height'}" FontWeight="SemiBold" />
                                                <TextBox Text="{Binding Settings.AvatarScaleSafety.CurrentMaximumHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                            </StackPanel>
                                        </UniformGrid>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <StackPanel Margin="0,0,0,12">
                            <TextBlock Text="{loc:Translate 'Twitch Reward Scaling'}"
                                       FontFamily="{DynamicResource HeadingFontFamily}"
                                       FontWeight="Bold"
                                       FontSize="22"
                                        Foreground="{DynamicResource TitleBarTextBrush}" />
                            <TextBlock Margin="0,6,0,0"
                                       Text="{loc:Translate 'Set up channel-point rewards that change avatar height. Reward settings are kept together here, separate from paid support, cash payments, and Power Ups.'}"
                                        Foreground="{DynamicResource TitleBarSubTextBrush}" />
                        </StackPanel>

                        <Border>
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource CardGroupStyle}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ActiveSourceView}" Value="TwitchRewards">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding ActiveSourceView}" Value="AllSources">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <StackPanel>
                                    <TextBlock Text="{loc:Translate 'Master Unlock Reward'}" FontWeight="Bold" FontSize="16" />
                                    <TextBlock Margin="0,4,0,10"
                                               Text="{loc:Translate 'The master reward can unlock child scale rewards while the shared safety cap still applies.'}"
                                               Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                    <ContentControl Content="{Binding MasterRewardCard}" ContentTemplate="{StaticResource SourceCardTemplate}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Two-column layout: Channel-Point Redeems | Pay System Rewards -->
                        <Grid x:Name="TwoColumnLayoutGrid">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="ChannelPointColumn" Width="*" />
                                <ColumnDefinition x:Name="PaySystemSpacerColumn" Width="12" />
                                <ColumnDefinition x:Name="PaySystemColumn" Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- LEFT COLUMN: Child Scale Rewards (channel-point redeems) -->
                            <Border Grid.Column="0">
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource CardGroupStyle}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ActiveSourceView}" Value="TwitchRewards">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding ActiveSourceView}" Value="AllSources">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <DockPanel LastChildFill="True">
                                        <Button DockPanel.Dock="Right"
                                                Content="{loc:Translate 'Add Scale Redeem'}"
                                                Command="{Binding AddAvatarScaleRuleCommand}"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Margin="8,0,0,0" />
                                        <TextBlock Text="{loc:Translate 'Child Scale Rewards'}" FontWeight="Bold" FontSize="16" />
                                    </DockPanel>
                                    <TextBlock Margin="0,4,0,10"
                                               Text="{loc:Translate 'Channel point rewards and chat command fallbacks that change avatar height.'}"
                                               Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                    <ItemsControl ItemsSource="{Binding TwitchScaleSetGroups}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:AvatarScalingScaleSetGroupViewModel}">
                                                <Border Margin="0,0,0,12"
                                                        Padding="10"
                                                        CornerRadius="14"
                                                        Background="{DynamicResource PanelSecondaryBrush}"
                                                        BorderBrush="{DynamicResource BorderBrush}"
                                                        BorderThickness="1">
                                                    <StackPanel>
                                                        <DockPanel LastChildFill="True">
                                                            <TextBlock DockPanel.Dock="Right" Text="{Binding CountText}" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TitleBarTextBrush}" />
                                                        </DockPanel>
                                                        <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding Cards}" ItemTemplate="{StaticResource SourceCardTemplate}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <UniformGrid Columns="2" />
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemContainerStyle>
                                                                <Style TargetType="ContentPresenter">
                                                                    <Setter Property="Margin" Value="0,0,8,8" />
                                                                </Style>
                                                            </ItemsControl.ItemContainerStyle>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- RIGHT COLUMN: Pay System Rewards -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="{loc:Translate 'Pay System Rewards'}" FontWeight="Bold" FontSize="16" />
                                <TextBlock Margin="0,4,0,10"
                                           Text="{loc:Translate 'Supporter Growth, Cash Payments &amp; Power Ups'}"
                                           Foreground="{DynamicResource TitleBarSubTextBrush}" />

                                <!-- Supporter Growth -->
                                <Border>
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource CardGroupStyle}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ActiveSourceView}" Value="SupporterGrowth">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActiveSourceView}" Value="AllSources">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <DockPanel LastChildFill="True">
                                            <Button DockPanel.Dock="Right"
                                                    Content="{loc:Translate 'Add Supporter Growth'}"
                                                    Command="{Binding AddRewardGrowthCommand}"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Margin="8,0,0,0" />
                                            <TextBlock Text="{loc:Translate 'Supporter Growth'}" FontWeight="Bold" FontSize="14" />
                                        </DockPanel>
                                        <TextBlock Margin="0,4,0,10"
                                                   Text="{loc:Translate 'Event-driven Bits and Subs growth rules.'}"
                                                   Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                        <ItemsControl ItemsSource="{Binding SupporterGrowthCards}" ItemTemplate="{StaticResource SourceCardTemplate}" />
                                    </StackPanel>
                                </Border>

                                <!-- Cash Payments -->
                                <Border>
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource CardGroupStyle}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ActiveSourceView}" Value="CashPayments">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActiveSourceView}" Value="AllSources">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <DockPanel LastChildFill="True">
                                            <Button DockPanel.Dock="Right"
                                                    Content="{loc:Translate 'Add Cash Payment'}"
                                                    Command="{Binding AddAvatarScalingCashPaymentRuleCommand}"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Margin="8,0,0,0" />
                                            <TextBlock Text="{loc:Translate 'Cash Payments'}" FontWeight="Bold" FontSize="14" />
                                        </DockPanel>
                                        <TextBlock Margin="0,4,0,10"
                                                   Text="{loc:Translate 'StreamElements, Streamlabs, and Ko-fi payment scaling rules.'}"
                                                   Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                        <ItemsControl ItemsSource="{Binding CashPaymentCards}" ItemTemplate="{StaticResource SourceCardTemplate}" />
                                    </StackPanel>
                                </Border>

                                <!-- Power Ups -->
                                <Border>
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource CardGroupStyle}">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ActiveSourceView}" Value="PowerUps">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ActiveSourceView}" Value="AllSources">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel>
                                        <DockPanel LastChildFill="True">
                                            <Button DockPanel.Dock="Right"
                                                    Content="{loc:Translate 'Add Power Up'}"
                                                    Command="{Binding AddAvatarScalingPowerUpRuleCommand}"
                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                    Margin="8,0,0,0" />
                                            <TextBlock Text="{loc:Translate 'Power Ups'}" FontWeight="Bold" FontSize="14" />
                                        </DockPanel>
                                        <TextBlock Margin="0,4,0,10"
                                                   Text="{loc:Translate 'Twitch Power-up Bits scaling rules.'}"
                                                   Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                        <ItemsControl ItemsSource="{Binding PowerUpCards}" ItemTemplate="{StaticResource SourceCardTemplate}" />
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Column="3"
                        Background="{DynamicResource PanelBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="16"
                        Padding="14"
                        Margin="12,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" TextElement.Foreground="{DynamicResource TitleBarTextBrush}">
                            <DockPanel LastChildFill="True">
                                <Button DockPanel.Dock="Right"
                                        Command="{Binding CloseEditorCommand}"
                                        Style="{StaticResource SecondaryButtonStyle}"
                                        AutomationProperties.Name="{loc:Translate 'Close'}"
                                        ToolTip="{loc:Translate 'Close'}">
                                    <Path Width="10" Height="10" Stretch="Uniform" Stroke="{DynamicResource TitleBarTextBrush}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round" Data="M 0,0 L 10,10 M 10,0 L 0,10" />
                                </Button>
                                <StackPanel>
                                    <TextBlock Text="{loc:Translate 'Editing child reward'}" Foreground="{DynamicResource TitleBarSubTextBrush}" FontSize="11" />
                                    <TextBlock Text="{Binding SelectedCard.Title}" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource TitleBarTextBrush}" />
                                </StackPanel>
                            </DockPanel>

                            <Border Margin="0,12,0,0"
                                    Padding="12"
                                    CornerRadius="14"
                                    Background="{DynamicResource NestedPanelBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    TextElement.Foreground="{DynamicResource TitleBarTextBrush}">
                                <StackPanel>
                                    <TextBlock Text="{loc:Translate 'Selected Source'}" FontWeight="Bold" />
                                    <TextBlock Margin="0,6,0,0" Text="{Binding SelectedCard.ActionSummary}" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                    <TextBlock Margin="0,8,0,0" Text="{Binding SelectedCard.SafetySummary}" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <ScrollViewer Grid.Row="1" Margin="0,12,0,0" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <Border Padding="12"
                                        CornerRadius="14"
                                        Background="{DynamicResource NestedPanelBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        TextElement.Foreground="{DynamicResource TitleBarTextBrush}"
                                        Visibility="{Binding HasNoSelectedCard, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Translate 'Selected Source'}" FontWeight="Bold" FontSize="15" />
                                        <TextBlock Margin="0,6,0,0"
                                                   Text="{loc:Translate 'Choose which Avatar Scaling source type to review.'}"
                                                   Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                    </StackPanel>
                                </Border>

                                <ContentControl Content="{Binding SelectedCard.MasterReward}">
                                    <ContentControl.Style>
                                        <Style TargetType="ContentControl">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SelectedCard.MasterReward}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                    <ContentControl.ContentTemplate>
                                        <DataTemplate DataType="{x:Type models:AvatarScaleMasterRewardSettings}">
                                            <Border Padding="12"
                                                    CornerRadius="14"
                                                    Background="{DynamicResource NestedPanelBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1"
                                                    TextElement.Foreground="{DynamicResource TitleBarTextBrush}">
                                                <StackPanel>
                                                    <TextBlock Text="{loc:Translate 'Avatar Scaling Master Reward'}" FontWeight="Bold" FontSize="15" />
                                                    <TextBlock Margin="0,6,0,0"
                                                               Text="{loc:Translate 'The master reward temporarily unlocks Avatar Scaling channel-point rewards so they do not need to stay visible on Twitch all the time. Supporter Growth is not affected because it listens to bits and subs directly.'}"
                                                               Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                                    <CheckBox Margin="0,10,0,0"
                                                              Content="{loc:Translate 'Enable Master Reward'}"
                                                              IsChecked="{Binding IsEnabled, UpdateSourceTrigger=PropertyChanged}" />
                                                    <UniformGrid Columns="2" Margin="0,10,0,0">
                                                        <StackPanel Margin="0,0,6,0">
                                                            <TextBlock Text="{loc:Translate 'Reward Title'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding RewardTitle, UpdateSourceTrigger=PropertyChanged}" />
                                                        </StackPanel>
                                                        <StackPanel Margin="6,0,0,0">
                                                            <TextBlock Text="{loc:Translate 'Reward Cost'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding RewardCost, UpdateSourceTrigger=PropertyChanged}" />
                                                        </StackPanel>
                                                    </UniformGrid>
                                                    <UniformGrid Columns="2" Margin="0,10,0,0">
                                                        <StackPanel Margin="0,0,6,0">
                                                            <TextBlock Text="{loc:Translate 'Unlock Duration Seconds'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding UnlockDurationSeconds, UpdateSourceTrigger=PropertyChanged}" />
                                                        </StackPanel>
                                                        <StackPanel Margin="6,0,0,0">
                                                            <TextBlock Text="{loc:Translate 'Cooldown Seconds'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding CooldownSeconds, UpdateSourceTrigger=PropertyChanged}" />
                                                        </StackPanel>
                                                    </UniformGrid>
                                                    <CheckBox Margin="0,10,0,0"
                                                              Content="{loc:Translate 'Free child reward slots while locked'}"
                                                              IsChecked="{Binding FreeChildRewardSlotsWhenLocked, UpdateSourceTrigger=PropertyChanged}" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ContentControl.ContentTemplate>
                                </ContentControl>

                                <ContentControl Content="{Binding SelectedAvatarScaleRule}">
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedAvatarScaleRule}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                                <ContentControl.ContentTemplate>
                                    <DataTemplate DataType="{x:Type models:AvatarScaleRule}">
                                        <StackPanel TextElement.Foreground="{DynamicResource TitleBarTextBrush}">
                                            <Border Padding="12"
                                                    CornerRadius="14"
                                                    Background="{DynamicResource NestedPanelBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1">
                                                <StackPanel>
                                                    <TextBlock Text="{loc:Translate 'Twitch Reward'}" FontWeight="Bold" FontSize="15" />
                                                    <TextBlock Margin="0,6,0,0" Text="{Binding TriggerSummary}" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                                    <StackPanel Margin="0,10,0,0">
                                                        <TextBlock Text="{loc:Translate 'Trigger Type'}" FontWeight="SemiBold" />
                                                        <ComboBox ItemsSource="{Binding DataContext.AvailableAvatarScaleTriggerTypesForSelectedRule, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                  SelectedItem="{Binding TriggerType, UpdateSourceTrigger=PropertyChanged}" />
                                                    </StackPanel>

                                                    <StackPanel Margin="0,10,0,0">
                                                        <StackPanel.Style>
                                                            <Style TargetType="StackPanel">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding UsesChatCommand}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Style>
                                                        <TextBlock Text="{loc:Translate 'Chat Command'}" FontWeight="SemiBold" />
                                                        <UniformGrid Columns="2" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Command Text'}" FontWeight="SemiBold" />
                                                                <TextBox Text="{Binding CommandText, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Permission'}" FontWeight="SemiBold" />
                                                                <ComboBox ItemsSource="{Binding DataContext.ChatCommandPermissionOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                          DisplayMemberPath="Label"
                                                                          SelectedValuePath="Value"
                                                                          SelectedValue="{Binding ChatCommandPermission, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                    </StackPanel>

                                                    <StackPanel Margin="0,10,0,0">
                                                        <StackPanel.Style>
                                                            <Style TargetType="StackPanel">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding UsesBits}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Style>
                                                        <TextBlock Text="{loc:Translate 'Bits Range'}" FontWeight="SemiBold" />
                                                        <TextBlock Margin="0,6,0,0"
                                                                   Text="{loc:Translate 'Use minimum and maximum bits to decide which cheer amounts trigger this scale redeem.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                                        <UniformGrid Columns="2" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Minimum Bits'}" FontWeight="SemiBold" />
                                                                <TextBox Text="{Binding MinimumBits, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Maximum Bits'}" FontWeight="SemiBold" />
                                                                <TextBox Text="{Binding MaximumBits, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                    </StackPanel>

                                                    <StackPanel Margin="0,10,0,0">
                                                        <StackPanel.Style>
                                                            <Style TargetType="StackPanel">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding UsesSubscription}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Style>
                                                        <TextBlock Text="{loc:Translate 'Sub/Gift Sub Filter'}" FontWeight="SemiBold" />
                                                        <TextBlock Margin="0,6,0,0"
                                                                   Text="{loc:Translate 'Pick a Twitch sub tier. Months narrow how long the viewer has been subscribed; use -1 for both month fields to match any month count.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                                        <ComboBox Margin="0,8,0,0"
                                                                  ItemsSource="{Binding DataContext.AvatarScaleSubscriptionTierOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                  DisplayMemberPath="Label"
                                                                  SelectedValuePath="Value"
                                                                  SelectedValue="{Binding SubscriptionTier, UpdateSourceTrigger=PropertyChanged}" />
                                                        <UniformGrid Columns="2" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Min Months'}" FontWeight="SemiBold" />
                                                                <TextBox Text="{Binding MinimumMonths, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Max Months'}" FontWeight="SemiBold" />
                                                                <TextBox Text="{Binding MaximumMonths, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                    </StackPanel>

                                                    <StackPanel Margin="0,10,0,0">
                                                        <StackPanel.Style>
                                                            <Style TargetType="StackPanel">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding UsesFollow}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Style>
                                                        <TextBlock Text="{loc:Translate 'Follow'}" FontWeight="SemiBold" />
                                                        <TextBlock Margin="0,6,0,0" Text="{Binding TriggerSummary}" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                                    </StackPanel>

                                                    <StackPanel Margin="0,10,0,0">
                                                        <StackPanel.Style>
                                                            <Style TargetType="StackPanel">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding UsesChannelPointReward}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </StackPanel.Style>
                                                        <TextBlock Text="{loc:Translate 'Reward Source'}" FontWeight="SemiBold" />
                                                        <ComboBox ItemsSource="{Binding DataContext.RewardSyncModeOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                  DisplayMemberPath="Label"
                                                                  SelectedValuePath="Value"
                                                                  SelectedValue="{Binding RewardSyncMode, UpdateSourceTrigger=PropertyChanged}" />

                                                        <StackPanel Margin="0,10,0,0" Visibility="{Binding UsesLinkedExistingReward, Converter={StaticResource BoolToVisibilityConverter}}">
                                                            <TextBlock Text="{loc:Translate 'Existing Twitch Reward'}" FontWeight="SemiBold" />
                                                            <ComboBox ItemsSource="{Binding DataContext.RewardOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                      DisplayMemberPath="DisplayLabel"
                                                                      SelectedValuePath="Id"
                                                                      SelectedValue="{Binding RewardId, UpdateSourceTrigger=PropertyChanged}" />
                                                            <WrapPanel Margin="0,8,0,0">
                                                                <Button Content="{loc:Translate 'Refresh Rewards'}"
                                                                        Command="{Binding DataContext.RefreshTwitchRewardsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                        Style="{StaticResource SecondaryButtonStyle}" />
                                                                <Button Content="{loc:Translate 'Unlink Reward'}"
                                                                        Command="{Binding DataContext.UnlinkTwitchRewardCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                        CommandParameter="{Binding}"
                                                                        Style="{StaticResource SecondaryButtonStyle}" />
                                                            </WrapPanel>
                                                        </StackPanel>

                                                        <UniformGrid Columns="2" Margin="0,10,0,0">
                                                            <UniformGrid.Style>
                                                                <Style TargetType="UniformGrid">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding UsesLinkedExistingReward}" Value="True">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </UniformGrid.Style>
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Reward Name'}" FontWeight="SemiBold" />
                                                                <TextBox Text="{Binding RewardTitle, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Reward Cost'}" FontWeight="SemiBold" />
                                                                <TextBox Text="{Binding RewardCost, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <StackPanel Margin="0,10,0,0">
                                                            <StackPanel.Style>
                                                                <Style TargetType="StackPanel">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding UsesLinkedExistingReward}" Value="True">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </StackPanel.Style>
                                                            <TextBlock Text="{loc:Translate 'Reward Description'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding RewardDescription, UpdateSourceTrigger=PropertyChanged}"
                                                                     AcceptsReturn="True"
                                                                     Height="64"
                                                                     TextWrapping="Wrap" />
                                                        </StackPanel>
                                                        <StackPanel Margin="0,10,0,0">
                                                            <StackPanel.Style>
                                                                <Style TargetType="StackPanel">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding UsesLinkedExistingReward}" Value="True">
                                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </StackPanel.Style>
                                                            <TextBlock Text="{loc:Translate 'Managed Reward Colors'}" FontWeight="SemiBold" />
                                                            <DockPanel Margin="0,6,0,0" LastChildFill="False">
                                                                <Border Width="24" Height="24" CornerRadius="12" Background="{Binding ManagedRewardReadyColorBrush}" BorderBrush="{DynamicResource InputBorderBrush}" BorderThickness="1" />
                                                                <Button Margin="10,0,0,0" Content="{loc:Translate 'Ready Color'}" Click="OnPickManagedRewardColorClicked" Tag="Ready" Style="{StaticResource SecondaryButtonStyle}" />
                                                                <Border Margin="14,0,0,0" Width="24" Height="24" CornerRadius="12" Background="{Binding ManagedRewardCooldownColorBrush}" BorderBrush="{DynamicResource InputBorderBrush}" BorderThickness="1" />
                                                                <Button Margin="10,0,0,0" Content="{loc:Translate 'Cooldown Color'}" Click="OnPickManagedRewardColorClicked" Tag="Cooldown" Style="{StaticResource SecondaryButtonStyle}" />
                                                            </DockPanel>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>

                                            <Border Margin="0,12,0,0"
                                                    Padding="12"
                                                    CornerRadius="14"
                                                    Background="{DynamicResource NestedPanelBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1">
                                                <StackPanel>
                                                    <TextBlock Text="{loc:Translate 'Height Change'}" FontWeight="Bold" FontSize="15" />
                                                    <UniformGrid Columns="3" Margin="0,10,0,0">
                                                        <Button Content="{loc:Translate 'Set Height'}" Tag="SetHeight" Click="ScaleActionModeButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                        <Button Content="{loc:Translate 'Random'}" Tag="RandomHeight" Click="ScaleActionModeButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                        <Button Content="{loc:Translate 'Relative'}" Tag="RelativeHeight" Click="ScaleActionModeButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                        <Button Content="{loc:Translate 'Multiplier'}" Tag="Multiplier" Click="ScaleActionModeButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                        <Button Content="{loc:Translate 'Preset'}" Tag="Preset" Click="ScaleActionModeButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                        <Button Content="{loc:Translate 'Glitchy'}" Tag="GlitchyRandomHeight" Click="ScaleActionModeButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                    </UniformGrid>

                                                    <StackPanel Margin="0,10,0,0" Visibility="{Binding UsesTargetHeight, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="{loc:Translate 'Target Height Meters'}" FontWeight="SemiBold" />
                                                        <TextBox Text="{Binding TargetHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                    </StackPanel>

                                                    <UniformGrid Columns="2" Margin="0,10,0,0"
                                                                 Visibility="{Binding UsesRandomHeightRange, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <StackPanel Margin="0,0,6,0">
                                                            <TextBlock Text="{loc:Translate 'Min Height (meters)'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding MinimumHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                        <StackPanel Margin="6,0,0,0">
                                                            <TextBlock Text="{loc:Translate 'Max Height (meters)'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding MaximumHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                    </UniformGrid>

                                                    <Grid Margin="0,10,0,0"
                                                          Visibility="{Binding UsesRelativeHeight, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <Button Grid.Column="0" Width="44" Content="{Binding RelativeHeightOperatorDisplay}" Click="ScaleActionRelHeightOpButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                        <StackPanel Grid.Column="1" Margin="8,0,6,0">
                                                            <TextBlock Text="{loc:Translate 'Change (meters)'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding RelativeHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                        <StackPanel Grid.Column="2" Margin="6,0,0,0">
                                                            <TextBlock Text="{loc:Translate 'Current (meters)'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding MultCurrentPreview, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                    </Grid>

                                                    <Grid Margin="0,10,0,0"
                                                          Visibility="{Binding UsesMultiplier, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <Button Grid.Column="0" Width="44" Content="{Binding MultiplierOperatorDisplay}" Click="ScaleActionMultOpButton_Click" Style="{StaticResource SecondaryButtonStyle}" />
                                                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                                            <TextBlock Text="{loc:Translate 'Multiplier'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding HeightMultiplier, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                    </Grid>

                                                    <StackPanel Margin="0,10,0,0"
                                                                Visibility="{Binding UsesPreset, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="{loc:Translate 'Preset'}" FontWeight="SemiBold" />
                                                        <ComboBox ItemsSource="{Binding DataContext.AvatarScalePresets, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                  SelectedItem="{Binding Preset, UpdateSourceTrigger=PropertyChanged}" />
                                                    </StackPanel>

                                                    <TextBlock Margin="0,10,0,0" Text="{loc:Translate 'Current Max Height Allowed'}" Foreground="{DynamicResource TitleBarSubTextBrush}" />
                                                    <TextBlock Text="{Binding DataContext.CurrentMaxHeightAllowedText, RelativeSource={RelativeSource AncestorType=Window}}" FontWeight="Bold" />
                                                </StackPanel>
                                            </Border>

                                            <Border Margin="0,12,0,0"
                                                    Padding="12"
                                                    CornerRadius="14"
                                                    Background="{DynamicResource NestedPanelBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1">
                                                <StackPanel>
                                                    <TextBlock Text="{loc:Translate 'Timer &amp; Return'}" FontWeight="Bold" FontSize="15" />
                                                    <UniformGrid Columns="2" Margin="0,10,0,0">
                                                        <StackPanel Margin="0,0,6,0">
                                                            <TextBlock Text="{loc:Translate 'Active Time Seconds'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding ActiveTimeSeconds, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                        <StackPanel Margin="6,0,0,0">
                                                            <TextBlock Text="{loc:Translate 'Return Height'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding RestoreHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                    </UniformGrid>
                                                    <UniformGrid Columns="2" Margin="0,10,0,0">
                                                        <StackPanel Margin="0,0,6,0">
                                                            <TextBlock Text="{loc:Translate 'Return Mode'}" FontWeight="SemiBold" />
                                                            <ComboBox ItemsSource="{Binding DataContext.AvatarScaleRestoreModes, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                      SelectedItem="{Binding RestoreMode, UpdateSourceTrigger=PropertyChanged}" />
                                                        </StackPanel>
                                                        <StackPanel Margin="6,0,0,0">
                                                            <TextBlock Text="{loc:Translate 'Smooth Transition Seconds'}" FontWeight="SemiBold" />
                                                            <TextBox Text="{Binding SmoothTransitionSeconds, UpdateSourceTrigger=LostFocus}" />
                                                        </StackPanel>
                                                    </UniformGrid>

                                                    <StackPanel Margin="0,12,0,0" Visibility="{Binding UsesSupporterGrowth, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="{loc:Translate 'Supporter Growth'}" FontWeight="SemiBold" />
                                                        <TextBlock Margin="0,6,0,0"
                                                                   Text="{loc:Translate 'Supporter Growth listens to bits, new subs, resubs, and gift subs. Paid events add height and add fair active time, then return to normal when the paid timer ends.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}"
                                                                   TextWrapping="Wrap" />
                                                        <CheckBox Margin="0,10,0,0"
                                                                  Content="{loc:Translate 'Allow reward scale changes during paid growth'}"
                                                                  IsChecked="{Binding SupporterGrowthAllowRewardScaleOverlay, UpdateSourceTrigger=PropertyChanged}" />
                                                        <TextBlock Margin="26,6,0,0"
                                                                   Text="{loc:Translate 'When enabled, channel-point and chat scale redeems can temporarily adjust height during paid growth without changing the paid timer.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}"
                                                                   TextWrapping="Wrap" />
                                                        <UniformGrid Columns="2" Margin="0,10,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Normal Height'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthNormalHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Max Added Height'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthMaxAddedHeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                                <TextBlock Margin="0,6,0,0"
                                                                           Text="{loc:Translate 'Use 0 for unlimited added height until VRChat or safe range clamps it.'}"
                                                                           Foreground="{DynamicResource TitleBarSubTextBrush}"
                                                                           TextWrapping="Wrap" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <TextBlock Margin="0,14,0,0"
                                                                   Text="{loc:Translate 'Paid Active Time'}"
                                                                   FontWeight="SemiBold" />
                                                        <TextBlock Margin="0,6,0,0"
                                                                   Text="{loc:Translate 'Paid time is shared by bits, subs, resubs, and gift subs. Time adds to the remaining paid timer, then slows above the soft cap and never exceeds the max.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}"
                                                                   TextWrapping="Wrap" />
                                                        <UniformGrid Columns="3" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Bits Timer Unit'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthBitsTimerUnit, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Seconds Per Bits Unit'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthSecondsPerBitsUnit, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Soft Cap Seconds'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthSoftCapSeconds, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <UniformGrid Columns="3" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Soft Cap Multiplier Percent'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthSoftCapMultiplierPercent, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Max Paid Time Seconds'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthMaxPaidTimeSeconds, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0" />
                                                        </UniformGrid>
                                                        <TextBlock Margin="0,14,0,0"
                                                                   Text="{loc:Translate 'Supporter Growth Cheer Keywords'}"
                                                                   FontWeight="SemiBold" />
                                                        <TextBlock Margin="0,6,0,0"
                                                                   Text="{loc:Translate 'Use cheer text like Cheer100 grow or Cheer100 shrink. No keyword keeps the existing positive growth behavior; if both words appear, Crystal Relay skips the scale instead of guessing.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}"
                                                                   TextWrapping="Wrap" />
                                                        <UniformGrid Columns="2" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Grow Keyword'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthGrowKeyword, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Shrink Keyword'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthShrinkKeyword, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <CheckBox Margin="0,10,0,0"
                                                                  Content="{loc:Translate 'Require grow or shrink keyword for height changes'}"
                                                                  IsChecked="{Binding SupporterGrowthRequireCheerKeyword, UpdateSourceTrigger=PropertyChanged}" />
                                                        <TextBlock Margin="26,6,0,0"
                                                                   Text="{loc:Translate 'When enabled, bits only change height if the cheer message says your grow or shrink keyword. Bits without a keyword still add paid time, but do not grow or shrink the avatar.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}"
                                                                   TextWrapping="Wrap" />
                                                        <TextBlock Margin="0,14,0,0"
                                                                   Text="{loc:Translate 'Subscription Growth'}"
                                                                   FontWeight="SemiBold" />
                                                        <UniformGrid Columns="3" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Tier 1 Height Add'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthTier1HeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Tier 2 Height Add'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthTier2HeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Tier 3 Height Add'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthTier3HeightMeters, UpdateSourceTrigger=LostFocus}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <TextBlock Margin="0,14,0,0"
                                                                   Text="{loc:Translate 'Subscription Paid Time'}"
                                                                   FontWeight="SemiBold" />
                                                        <UniformGrid Columns="3" Margin="0,8,0,0">
                                                            <StackPanel Margin="0,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Tier 1 Seconds'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthTier1Seconds, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,6,0">
                                                                <TextBlock Text="{loc:Translate 'Tier 2 Seconds'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthTier2Seconds, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                            <StackPanel Margin="6,0,0,0">
                                                                <TextBlock Text="{loc:Translate 'Tier 3 Seconds'}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBox Text="{Binding SupporterGrowthTier3Seconds, UpdateSourceTrigger=PropertyChanged}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <DockPanel Margin="0,14,0,0" LastChildFill="False">
                                                            <TextBlock Text="{loc:Translate 'Bits Growth Ranges'}" FontWeight="SemiBold" />
                                                            <Button Margin="10,0,0,0" Content="{loc:Translate 'Add Bits Range'}" Click="OnAddSupporterGrowthBitRangeClicked" Style="{StaticResource SecondaryButtonStyle}" />
                                                        </DockPanel>
                                                        <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding SupporterGrowthBitRanges}">
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate DataType="{x:Type models:AvatarScaleBitGrowthRange}">
                                                                    <Grid Margin="0,0,0,8">
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="*" />
                                                                            <ColumnDefinition Width="Auto" />
                                                                        </Grid.ColumnDefinitions>
                                                                        <TextBox Grid.Column="0" Margin="0,0,4,0" Text="{Binding MinimumBits, UpdateSourceTrigger=PropertyChanged}" />
                                                                        <TextBox Grid.Column="1" Margin="4,0,4,0" Text="{Binding MaximumBits, UpdateSourceTrigger=PropertyChanged}" />
                                                                        <TextBox Grid.Column="2" Margin="4,0,4,0" Text="{Binding HeightAddedMeters, UpdateSourceTrigger=LostFocus}" />
                                                                        <Button Grid.Column="3" Content="{loc:Translate 'Remove'}" Click="OnRemoveSupporterGrowthBitRangeClicked" Style="{StaticResource SecondaryButtonStyle}" />
                                                                    </Grid>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                        <TextBlock Margin="0,6,0,0"
                                                                   Text="{loc:Translate 'Maximum Bits set to 0 means no upper limit for that row.'}"
                                                                   Foreground="{DynamicResource TitleBarSubTextBrush}"
                                                                   TextWrapping="Wrap" />
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>

                                            <Border Background="{DynamicResource NestedPanelBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="10"
                                                    Padding="10"
                                                    Margin="0,8,0,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding UsesSupporterGrowth}" Value="True">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <StackPanel>
                                                    <CheckBox IsChecked="{Binding ExtendCurrentActivity, Mode=TwoWay}"
                                                              Content="{loc:Translate &quot;Extend the current active activity instead of running this rule&apos;s action&quot;}"
                                                              Margin="0,0,0,6" />
                                                    <StackPanel Orientation="Horizontal"
                                                                Visibility="{Binding ExtendCurrentActivity, Converter={StaticResource BoolToVisibilityConverter}}">
                                                        <TextBlock Text="{loc:Translate 'Extend by (seconds)'}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0,0,8,0" />
                                                        <TextBox Text="{Binding ExtendSeconds, UpdateSourceTrigger=PropertyChanged}"
                                                                 MinWidth="80" />
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>


                                        </StackPanel>
                                    </DataTemplate>
                                </ContentControl.ContentTemplate>
                                </ContentControl>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>
</Window>
