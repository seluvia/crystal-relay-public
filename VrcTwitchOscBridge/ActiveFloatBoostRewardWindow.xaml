<Window x:Class="VrcTwitchOscBridge.ActiveFloatBoostRewardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:VrcTwitchOscBridge.Services"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="{loc:Translate 'Active Float Boost Reward | Crystal Relay'}"
        Icon="Assets/crystal-relay-icon.ico"
        Width="680"
        Height="640"
        MinWidth="680"
        MinHeight="640"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource BodyFontFamily}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{DynamicResource WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <FontFamily x:Key="BodyFontFamily">Verdana</FontFamily>
        <FontFamily x:Key="HeadingFontFamily">Constantia</FontFamily>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#130B1E" />
        <SolidColorBrush x:Key="PanelBrush" Color="#CC1C132B" />
        <SolidColorBrush x:Key="PanelSecondaryBrush" Color="#C8241739" />
        <SolidColorBrush x:Key="BorderBrush" Color="#4B2B78" />
        <SolidColorBrush x:Key="AccentBrush" Color="#A855F7" />
        <SolidColorBrush x:Key="TextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="MutedBrush" Color="#C9B8E3" />
        <SolidColorBrush x:Key="InputBrush" Color="#B8271A3D" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#5B3A8E" />
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#2C1C48" />
        <SolidColorBrush x:Key="SecondaryButtonBorderBrush" Color="#6942A7" />
        <SolidColorBrush x:Key="TitleBarBrush" Color="#20122F" />
        <SolidColorBrush x:Key="TitleBarTextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="TitleBarSubTextBrush" Color="#CBB9E5" />
        <SolidColorBrush x:Key="TitleBarButtonBrush" Color="#00000000" />
        <SolidColorBrush x:Key="TitleBarButtonHoverBrush" Color="#3B235B" />
        <SolidColorBrush x:Key="TitleBarButtonPressedBrush" Color="#543183" />
        <SolidColorBrush x:Key="TitleBarCloseHoverBrush" Color="#B43D62" />
        <SolidColorBrush x:Key="TitleBarClosePressedBrush" Color="#8C2648" />

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,6,0,0" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="FontFamily" Value="{DynamicResource HeadingFontFamily}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="15,10" />
            <Setter Property="MinWidth" Value="116" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{DynamicResource TitleBarButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TitleBarTextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Border Margin="1"
            Background="{DynamicResource WindowBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{DynamicResource TitleBarBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="OnTitleBarMouseLeftButtonDown">
                <Grid Margin="12,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <DockPanel VerticalAlignment="Center">
                        <Image Source="Assets/crystal-relay-icon.png"
                               Width="18"
                               Height="18"
                               Margin="0,0,10,0"
                               Stretch="Uniform" />
                        <StackPanel Orientation="Horizontal"
                                    VerticalAlignment="Center">
                            <TextBlock Text="{loc:Translate 'Active Float Boost Reward'}"
                                       FontFamily="{DynamicResource HeadingFontFamily}"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource TitleBarTextBrush}" />
                            <TextBlock Margin="8,0,0,0"
                                       Text="{loc:Translate 'Crystal Relay'}"
                                       Foreground="{DynamicResource TitleBarSubTextBrush}"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </DockPanel>
                    <Button Grid.Column="1"
                            Style="{StaticResource TitleBarButtonStyle}"
                            Click="OnCloseButtonClicked">
                        <TextBlock Text="&#x2715;"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TitleBarTextBrush}" />
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Margin="20"
                    Padding="20"
                    Background="{DynamicResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock x:Name="RuleNameTextBlock"
                                   FontFamily="{DynamicResource HeadingFontFamily}"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   TextWrapping="Wrap" />
                        <TextBlock Margin="0,6,0,16"
                                   Text="{loc:Translate 'The boost reward appears while this timed float redeem is active. Each redeem adds to the current float value and refreshes the active timer.'}"
                                   Foreground="{DynamicResource MutedBrush}"
                                   TextWrapping="Wrap" />

                        <CheckBox Content="{loc:Translate 'Enable active boost reward'}"
                                  IsChecked="{Binding ActiveFloatBoostRewardEnabled, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock Margin="0,8,0,0"
                                   Text="{Binding ActiveFloatBoostRewardStatusText}"
                                   Foreground="{DynamicResource MutedBrush}"
                                   TextWrapping="Wrap" />

                        <StackPanel Margin="0,16,0,0"
                                    Visibility="{Binding ActiveFloatBoostRewardEnabled, Converter={StaticResource BoolToVisibilityConverter}}">
                            <TextBlock Text="{loc:Translate 'Boost Reward Title'}"
                                       FontWeight="SemiBold" />
                            <TextBox Text="{Binding ActiveFloatBoostRewardTitle, UpdateSourceTrigger=PropertyChanged}" />

                            <UniformGrid Columns="2"
                                         Margin="0,12,0,0">
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="{loc:Translate 'Reward Cost'}"
                                               FontWeight="SemiBold" />
                                    <TextBox Text="{Binding ActiveFloatBoostRewardCost, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Margin="10,0,0,0">
                                    <TextBlock Text="{loc:Translate 'Cooldown (seconds)'}"
                                               FontWeight="SemiBold" />
                                    <TextBox Text="{Binding ActiveFloatBoostRewardCooldownSeconds, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </UniformGrid>

                            <TextBlock Margin="0,12,0,0"
                                       Text="{loc:Translate 'Reward Description'}"
                                       FontWeight="SemiBold" />
                            <TextBox Text="{Binding ActiveFloatBoostRewardDescription, UpdateSourceTrigger=PropertyChanged}"
                                     MinHeight="68"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True" />

                            <UniformGrid Columns="3"
                                         Margin="0,12,0,0">
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="{loc:Translate 'Add Amount'}"
                                               FontWeight="SemiBold" />
                                    <TextBox Text="{Binding ActiveFloatBoostAddValue, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Margin="8,0,8,0">
                                    <TextBlock Text="{loc:Translate 'Minimum Value'}"
                                               FontWeight="SemiBold" />
                                    <TextBox Text="{Binding ActiveFloatBoostMinimumValue, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                                <StackPanel Margin="8,0,0,0">
                                    <TextBlock Text="{loc:Translate 'Maximum Value'}"
                                               FontWeight="SemiBold" />
                                    <TextBox Text="{Binding ActiveFloatBoostMaximumValue, UpdateSourceTrigger=PropertyChanged}" />
                                </StackPanel>
                            </UniformGrid>

                            <TextBlock Margin="0,8,0,0"
                                       Text="{Binding FloatValueModeHelpText}"
                                       Foreground="{DynamicResource MutedBrush}"
                                       TextWrapping="Wrap" />

                            <UniformGrid Columns="2"
                                         Margin="0,12,0,0">
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="{loc:Translate 'Ready Color'}"
                                               FontWeight="SemiBold" />
                                    <DockPanel Margin="0,6,0,0"
                                               LastChildFill="True">
                                        <Border DockPanel.Dock="Left"
                                                Width="28"
                                                Height="28"
                                                CornerRadius="14"
                                                Background="{Binding ActiveFloatBoostRewardReadyColorBrush}"
                                                BorderBrush="{DynamicResource InputBorderBrush}"
                                                BorderThickness="1" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{loc:Translate 'Choose Color'}"
                                                Padding="10,6"
                                                MinWidth="112"
                                                Click="OnPickActiveBoostRewardColorClicked"
                                                Tag="Ready" />
                                        <TextBox Margin="10,0,10,0"
                                                 Text="{Binding ActiveFloatBoostRewardReadyColor, UpdateSourceTrigger=LostFocus}" />
                                    </DockPanel>
                                </StackPanel>
                                <StackPanel Margin="10,0,0,0">
                                    <TextBlock Text="{loc:Translate 'Cooldown Color'}"
                                               FontWeight="SemiBold" />
                                    <DockPanel Margin="0,6,0,0"
                                               LastChildFill="True">
                                        <Border DockPanel.Dock="Left"
                                                Width="28"
                                                Height="28"
                                                CornerRadius="14"
                                                Background="{Binding ActiveFloatBoostRewardCooldownColorBrush}"
                                                BorderBrush="{DynamicResource InputBorderBrush}"
                                                BorderThickness="1" />
                                        <Button DockPanel.Dock="Right"
                                                Content="{loc:Translate 'Choose Color'}"
                                                Padding="10,6"
                                                MinWidth="112"
                                                Click="OnPickActiveBoostRewardColorClicked"
                                                Tag="Cooldown" />
                                        <TextBox Margin="10,0,10,0"
                                                 Text="{Binding ActiveFloatBoostRewardCooldownColor, UpdateSourceTrigger=LostFocus}" />
                                    </DockPanel>
                                </StackPanel>
                            </UniformGrid>
                        </StackPanel>

                        <Button Margin="0,20,0,0"
                                HorizontalAlignment="Right"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Content="{loc:Translate 'Done'}"
                                Click="OnCloseButtonClicked" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Border>
</Window>
