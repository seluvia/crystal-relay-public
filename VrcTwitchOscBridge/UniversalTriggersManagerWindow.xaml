<Window x:Class="VrcTwitchOscBridge.UniversalTriggersManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:VrcTwitchOscBridge.Services"
        xmlns:vm="clr-namespace:VrcTwitchOscBridge.ViewModels"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:models="clr-namespace:VrcTwitchOscBridge.Models"
        xmlns:local="clr-namespace:VrcTwitchOscBridge"
        xmlns:converters="clr-namespace:VrcTwitchOscBridge"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:UniversalTriggersManagerViewModel}"
        Title="{loc:Translate 'Universal Triggers'}"
        Icon="Assets/crystal-relay-icon.ico"
        Width="980"
        Height="640"
        MinWidth="720"
        MinHeight="480"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource BodyFontFamily}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="{DynamicResource WindowBackgroundBrush}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#130B1E" />
        <SolidColorBrush x:Key="PanelBrush" Color="#CC1C132B" />
        <SolidColorBrush x:Key="PanelSecondaryBrush" Color="#C8241739" />
        <SolidColorBrush x:Key="NestedPanelBrush" Color="#B8241739" />
        <SolidColorBrush x:Key="BorderBrush" Color="#4B2B78" />
        <SolidColorBrush x:Key="AccentBrush" Color="#A855F7" />
        <SolidColorBrush x:Key="AccentDimBrush" Color="#552D1B47" />
        <SolidColorBrush x:Key="TextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="MutedBrush" Color="#C9B8E3" />
        <SolidColorBrush x:Key="InputBrush" Color="#B8271A3D" />
        <SolidColorBrush x:Key="InputBorderBrush" Color="#5B3A8E" />
        <SolidColorBrush x:Key="ComboTextBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SecondaryButtonBrush" Color="#2C1C48" />
        <SolidColorBrush x:Key="SecondaryButtonBorderBrush" Color="#6942A7" />
        <SolidColorBrush x:Key="TitleBarBrush" Color="#20122F" />
        <SolidColorBrush x:Key="TitleBarTextBrush" Color="#F5EEFF" />
        <SolidColorBrush x:Key="TitleBarSubTextBrush" Color="#CBB9E5" />
        <SolidColorBrush x:Key="TitleBarButtonBrush" Color="#00000000" />
        <SolidColorBrush x:Key="TitleBarButtonHoverBrush" Color="#3B235B" />
        <SolidColorBrush x:Key="TitleBarButtonPressedBrush" Color="#543183" />
        <SolidColorBrush x:Key="TitleBarCloseHoverBrush" Color="#B43D62" />
        <SolidColorBrush x:Key="TitleBarClosePressedBrush" Color="#8C2648" />
        <SolidColorBrush x:Key="RuleCardHoverBrush" Color="#8D56E8" />
        <SolidColorBrush x:Key="StatusChipBrush" Color="#8E241841" />
        <SolidColorBrush x:Key="StatusStripeReadyBrush" Color="#4ADE80" />
        <SolidColorBrush x:Key="StatusStripeWarnBrush" Color="#FBBF24" />
        <SolidColorBrush x:Key="StatusStripeOffBrush" Color="#6B7280" />
        <SolidColorBrush x:Key="WarnBrush" Color="#7A4A1B" />
        <SolidColorBrush x:Key="WarnTextBrush" Color="#FBBF24" />
        <SolidColorBrush x:Key="WarnBorderBrush" Color="#A66A1F" />
        <SolidColorBrush x:Key="ScrollThumbBrush" Color="#8D56E8" />
        <SolidColorBrush x:Key="ScrollTrackBrush" Color="#1A0E22" />
        <SolidColorBrush x:Key="ComboSurfaceBrush" Color="#1F1530" />
        <SolidColorBrush x:Key="ComboHighlightBrush" Color="#3A2856" />
        <SolidColorBrush x:Key="ComboDropButtonBrush" Color="#2C1C48" />
        <SolidColorBrush x:Key="ComboDropButtonHoverBrush" Color="#3F2D5C" />
        <SolidColorBrush x:Key="ComboDropButtonPressedBrush" Color="#553080" />
        <SolidColorBrush x:Key="PopupBorderBrush" Color="#4B2B78" />
        <FontFamily x:Key="HeadingFontFamily">Constantia</FontFamily>
        <FontFamily x:Key="BodyFontFamily">Verdana</FontFamily>

        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <local:EnumBooleanConverter x:Key="EnumBooleanConverter" />
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        <local:CollapseChevronTextConverter x:Key="CollapseChevronTextConverter" />
        <local:CollapseChevronColorConverter x:Key="CollapseChevronColorConverter" />
        <local:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
        <local:BoolToHorizontalAlignmentConverter x:Key="BoolToHorizontalAlignmentConverter" />
        <local:BoolToThicknessConverter x:Key="BoolToThicknessConverter" />
        <converters:UniversalTriggerBoolConverter x:Key="UniversalTriggerBoolConverter" />
        <converters:UniversalTriggerIntConverter x:Key="UniversalTriggerIntConverter" />
        <converters:UniversalTriggerFloatConverter x:Key="UniversalTriggerFloatConverter" />
        <converters:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter" />

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
        </Style>
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="FilterChipToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource SecondaryButtonBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SecondaryButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="ChipBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="ChipBorder" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="ChipBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter Property="Foreground" Value="{DynamicResource ComboTextBrush}" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ChipBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbChrome"
                                Margin="2"
                                Background="{DynamicResource ScrollThumbBrush}"
                                CornerRadius="8" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbChrome" Property="Background" Value="{DynamicResource ComboDropButtonPressedBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ScrollBarTrackButtonStyle" TargetType="RepeatButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
            <Grid Width="{TemplateBinding Width}"
                  MinWidth="18"
                  Background="Transparent">
                <Border Background="{DynamicResource ScrollTrackBrush}"
                        BorderBrush="{DynamicResource InputBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="8" />
                <Track x:Name="PART_Track"
                       Margin="2"
                       IsDirectionReversed="True">
                    <Track.DecreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageUpCommand}" />
                    </Track.DecreaseRepeatButton>
                    <Track.Thumb>
                        <Thumb Style="{StaticResource ScrollBarThumbStyle}" />
                    </Track.Thumb>
                    <Track.IncreaseRepeatButton>
                        <RepeatButton Style="{StaticResource ScrollBarTrackButtonStyle}"
                                      Command="{x:Static ScrollBar.PageDownCommand}" />
                    </Track.IncreaseRepeatButton>
                </Track>
            </Grid>
        </ControlTemplate>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="{DynamicResource ScrollTrackBrush}" />
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}" />
        </Style>

        <Style x:Key="ComboBoxEditableTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style x:Key="ComboBoxToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False" />
            <Setter Property="Background" Value="{DynamicResource ComboDropButtonBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="DropButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0,11,11,0">
                            <Path x:Name="DropArrow"
                                  Width="11"
                                  Height="7"
                                  Stretch="Fill"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  Data="M 0 0 L 5.5 6 L 11 0"
                                  Stroke="{DynamicResource TextBrush}"
                                  StrokeThickness="1.8"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="DropButtonBorder" Property="Background" Value="{DynamicResource ComboDropButtonHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="DropButtonBorder" Property="Background" Value="{DynamicResource ComboDropButtonPressedBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="DropButtonBorder" Property="Background" Value="{DynamicResource ComboDropButtonHoverBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Background" Value="{DynamicResource ComboSurfaceBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="ComboBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="28" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="ContentSite"
                                                      Grid.Column="0"
                                                      Margin="10,0,4,0"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"
                                                      IsHitTestVisible="False" />
                                    <TextBox x:Name="PART_EditableTextBox"
                                             Grid.Column="0"
                                             Margin="10,0,4,0"
                                             Visibility="Hidden"
                                             Style="{StaticResource ComboBoxEditableTextBoxStyle}"
                                             IsReadOnly="{TemplateBinding IsReadOnly}" />
                                    <ToggleButton Grid.Column="1"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  Style="{StaticResource ComboBoxToggleButtonStyle}" />
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Margin="0,4,0,0"
                                            Padding="4"
                                            Background="{DynamicResource ComboSurfaceBrush}"
                                            BorderBrush="{DynamicResource PopupBorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="10">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ComboBorder" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Margin="0,1"
                                Padding="{TemplateBinding Padding}"
                                Background="Transparent"
                                CornerRadius="6">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboHighlightBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource ComboHighlightBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Background" Value="{DynamicResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource InputBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Border Width="18"
                                    Height="18"
                                    Margin="0,0,6,0"
                                    VerticalAlignment="Center"
                                    Background="Transparent">
                                <Grid>
                                    <Border x:Name="CheckChrome"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="5" />
                                    <Path x:Name="CheckMark"
                                          Width="10"
                                          Height="8"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 1 5 L 4.5 8 L 11 1"
                                          Stroke="White"
                                          StrokeThickness="2.2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Visibility="Collapsed" />
                                </Grid>
                            </Border>
                            <ContentPresenter Grid.Column="1"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              ContentSource="Content" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckChrome" Property="BorderBrush" Value="{DynamicResource RuleCardHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckChrome" Property="Background" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="CheckChrome" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="CheckChrome" Property="Opacity" Value="0.45" />
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="UniversalTriggerCardTemplate" DataType="{x:Type vm:UniversalTriggerCardViewModel}">
            <Border Width="240" Height="168" Margin="0,0,10,10"
                    Background="{DynamicResource PanelBrush}"
                    BorderBrush="{Binding StatusStripeBrush}"
                    BorderThickness="3,1,1,1"
                    CornerRadius="12"
                    Padding="10,12,10,12"
                    Cursor="Hand">
                <Border.InputBindings>
                    <MouseBinding MouseAction="LeftClick"
                                  Command="{Binding DataContext.OpenEditorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                  CommandParameter="{Binding Rule}" />
                </Border.InputBindings>
                <Grid x:Name="CardContent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="42" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="26" />
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" LastChildFill="True">
                        <Border DockPanel.Dock="Left" Width="36" Height="36" CornerRadius="10"
                                Background="{DynamicResource AccentDimBrush}">
                            <TextBlock Text="{Binding EmojiIcon}"
                                       FontSize="18"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center" />
                        </Border>
                        <Border DockPanel.Dock="Right"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="6,0,0,0"
                                Width="44"
                                Height="22"
                                CornerRadius="11"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#6B7280" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Rule.IsEnabled}" Value="True">
                                            <Setter Property="Background" Value="#22C55E" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <ToggleButton IsChecked="{Binding Rule.IsEnabled, Mode=TwoWay}"
                                              Cursor="Hand">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" />
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Ellipse Width="16"
                                         Height="16"
                                         Fill="White"
                                         IsHitTestVisible="False"
                                         VerticalAlignment="Center">
                                    <Ellipse.HorizontalAlignment>
                                        <Binding Path="Rule.IsEnabled" Converter="{StaticResource BoolToHorizontalAlignmentConverter}" />
                                    </Ellipse.HorizontalAlignment>
                                    <Ellipse.Margin>
                                        <Binding Path="Rule.IsEnabled" Converter="{StaticResource BoolToThicknessConverter}" />
                                    </Ellipse.Margin>
                                </Ellipse>
                            </Grid>
                        </Border>
                        <StackPanel Margin="8,0,6,0">
                            <TextBlock Text="{Binding Rule.Name}"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       Foreground="{DynamicResource TextBrush}"
                                       TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding Rule.Name}" />
                            <WrapPanel Margin="0,3,0,0">
                                <Border Background="{DynamicResource AccentBrush}" CornerRadius="8" Padding="6,1" Margin="0,0,4,0">
                                    <TextBlock Text="{Binding TypePill}"
                                               Foreground="{DynamicResource ComboTextBrush}"
                                               FontSize="8"
                                               FontWeight="Bold" />
                                </Border>
                                <Border Background="{Binding StatusStripeBrush}" CornerRadius="8" Padding="6,1" Margin="0,0,4,0">
                                    <TextBlock Text="{Binding StatusPill}"
                                               Foreground="{DynamicResource ComboTextBrush}"
                                               FontSize="8"
                                               FontWeight="Bold" />
                                </Border>
                                <Border Background="{DynamicResource StatusChipBrush}"
                                        CornerRadius="8"
                                        Padding="5,1"
                                        Margin="0,0,4,0"
                                        Visibility="{Binding IsFromFooma, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="pack://application:,,,/Assets/fooma-icon.png"
                                               Width="12" Height="12"
                                               Margin="0,0,3,0"
                                               VerticalAlignment="Center"
                                               RenderOptions.BitmapScalingMode="NearestNeighbor" />
                                        <TextBlock Text="{loc:Translate 'Universal Triggers Source Fooma'}"
                                                   Foreground="{DynamicResource MutedBrush}"
                                                   FontSize="8"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </Border>
                            </WrapPanel>
                        </StackPanel>
                    </DockPanel>

                    <TextBlock Grid.Row="1"
                               Text="{Binding Description}"
                               Foreground="{DynamicResource MutedBrush}"
                               FontSize="10"
                               TextWrapping="Wrap"
                               TextTrimming="CharacterEllipsis"
                               MaxHeight="50"
                               Margin="0,6,0,0" />

                    <Grid Grid.Row="2" Margin="0,4,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="5" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Padding="4,2"
                                FontSize="10"
                                Content="{loc:Translate 'Universal Triggers Card Test'}"
                                Command="{Binding DataContext.TestTriggerCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding Rule}" />
                        <Button Grid.Column="2"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Padding="4,2"
                                FontSize="10"
                                Content="{loc:Translate 'Universal Triggers Card Edit'}"
                                Command="{Binding DataContext.OpenEditorCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding Rule}" />
                    </Grid>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Disabled">
                    <Setter TargetName="CardContent" Property="Opacity" Value="0.55" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>
    <Border Margin="1"
            Background="{DynamicResource WindowBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource TitleBarBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="14,10"
                MouseLeftButtonDown="OnTitleBarMouseDown">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Right"
                        Content="✕"
                        Background="Transparent"
                        Foreground="{DynamicResource TitleBarTextBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        Padding="8,4"
                        Margin="6,0,0,0"
                        Click="OnCloseClicked"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True" />
                <StackPanel Orientation="Vertical">
                    <TextBlock Text="{loc:Translate '✨ Universal Triggers'}"
                               Foreground="{DynamicResource TitleBarTextBrush}"
                               FontWeight="Bold"
                               FontSize="14" />
                    <TextBlock Text="{Binding SubtitleSummary}"
                               Foreground="{DynamicResource TitleBarSubTextBrush}"
                               FontSize="11"
                               Margin="0,2,0,0" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="540">
                    <TextBlock Text="✨" FontSize="32" HorizontalAlignment="Center" />
                    <TextBlock Text="{loc:Translate 'Universal Triggers Welcome Title'}"
                               Foreground="{DynamicResource TextBrush}"
                               FontSize="18" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,8,0,0" />
                    <TextBlock Text="{loc:Translate 'Universal Triggers Welcome Body'}"
                               Foreground="{DynamicResource MutedBrush}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               Margin="0,6,0,0" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,22,0,0">

                        <Border Width="240" Padding="18" CornerRadius="14"
                                Background="{DynamicResource AccentDimBrush}"
                                BorderBrush="{DynamicResource AccentBrush}"
                                BorderThickness="1"
                                Margin="0,0,14,0">
                            <StackPanel>
                                <Image Source="pack://application:,,,/Assets/fooma-icon.png"
                                       Width="80" Height="80"
                                       HorizontalAlignment="Center"
                                       RenderOptions.BitmapScalingMode="NearestNeighbor" />
                                <TextBlock Text="{loc:Translate 'Universal Triggers Welcome Import Title'}"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           Margin="0,10,0,0" />
                                <TextBlock Text="{loc:Translate 'Universal Triggers Welcome Import Body'}"
                                           Foreground="{DynamicResource MutedBrush}"
                                           FontSize="11"
                                           TextWrapping="Wrap"
                                           TextAlignment="Center"
                                           Margin="0,6,0,0" />
                                <Button Content="{loc:Translate 'Universal Triggers Welcome Import Action'}"
                                        Command="{Binding ImportFoomaCommand}"
                                        Background="{DynamicResource AccentBrush}"
                                        Foreground="{DynamicResource ComboTextBrush}"
                                        FontWeight="Bold"
                                        Padding="10,6"
                                        Margin="0,12,0,0"
                                        HorizontalAlignment="Center" />
                            </StackPanel>
                        </Border>

                        <Border Width="240" Padding="18" CornerRadius="14"
                                Background="{DynamicResource PanelBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="🛠️" FontSize="48" Height="80"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                                <TextBlock Text="{loc:Translate 'Universal Triggers Welcome Create Title'}"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           Margin="0,10,0,0" />
                                <TextBlock Text="{loc:Translate 'Universal Triggers Welcome Create Body'}"
                                           Foreground="{DynamicResource MutedBrush}"
                                           FontSize="11"
                                           TextWrapping="Wrap"
                                           TextAlignment="Center"
                                           Margin="0,6,0,0" />
                                <Button Content="{loc:Translate 'Universal Triggers Welcome Create Action'}"
                                        Command="{Binding AddNewTriggerCommand}"
                                        Background="Transparent"
                                        Foreground="{DynamicResource TextBrush}"
                                        BorderBrush="{DynamicResource AccentBrush}"
                                        BorderThickness="1"
                                        Padding="10,6"
                                        Margin="0,12,0,0"
                                        HorizontalAlignment="Center" />
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </StackPanel>
            </Grid>

            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="{DynamicResource NestedPanelBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="14,8">
                    <DockPanel LastChildFill="False">
                        <Button DockPanel.Dock="Right"
                                Background="{DynamicResource AccentBrush}"
                                Foreground="{DynamicResource ComboTextBrush}"
                                FontWeight="Bold"
                                Padding="12,6"
                                Margin="8,0,0,0"
                                Content="{loc:Translate 'Universal Triggers New Trigger'}"
                                Command="{Binding AddNewTriggerCommand}" />
                        <Button DockPanel.Dock="Right"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Padding="10,4"
                                Margin="6,0,0,0"
                                Command="{Binding ImportFoomaCommand}">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="pack://application:,,,/Assets/fooma-icon.png"
                                       Width="18" Height="18"
                                       Margin="0,0,6,0"
                                       RenderOptions.BitmapScalingMode="NearestNeighbor" />
                                <TextBlock Text="{loc:Translate 'Universal Triggers Import Fooma'}"
                                           VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button DockPanel.Dock="Right"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Width="28"
                                Height="28"
                                Padding="0"
                                Margin="0,0,8,0"
                                ToolTip="Fooma import help"
                                Command="{Binding OpenFoomaHelpCommand}">
                            <TextBlock Text="?"
                                       FontWeight="Bold"
                                       FontSize="14"
                                       Foreground="{DynamicResource MutedBrush}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center" />
                        </Button>
                        <Grid DockPanel.Dock="Right" Width="220" Height="28" Margin="0,0,12,0">
                            <TextBox x:Name="SearchBox"
                                     Background="{DynamicResource InputBrush}"
                                     Foreground="{DynamicResource TextBrush}"
                                     BorderBrush="{DynamicResource InputBorderBrush}"
                                     VerticalContentAlignment="Center"
                                     Padding="28,4,8,4"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                            <TextBlock Text="🔍"
                                       Foreground="{DynamicResource MutedBrush}"
                                       FontSize="12"
                                       IsHitTestVisible="False"
                                       Margin="10,0,0,0"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Center" />
                            <TextBlock Text="Search triggers..."
                                       Foreground="{DynamicResource MutedBrush}"
                                       FontSize="11"
                                       IsHitTestVisible="False"
                                       Margin="30,0,8,0"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </DockPanel>
                </Border>

                <Border Grid.Row="1"
                        Background="{DynamicResource NestedPanelBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1"
                        Padding="14,8">
                    <DockPanel LastChildFill="False">
                        <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                    Margin="0,0,6,0"
                                    Padding="8,3"
                                    Content="{loc:Translate 'Universal Triggers Collapse All'}"
                                    Command="{Binding CollapseAllCommand}" />
                            <Button Style="{StaticResource SecondaryButtonStyle}"
                                    Margin="0,0,12,0"
                                    Padding="8,3"
                                    Content="{loc:Translate 'Universal Triggers Expand All'}"
                                    Command="{Binding ExpandAllCommand}" />
                            <ComboBox Width="160"
                                      Margin="0,0,12,0"
                                      Background="{DynamicResource InputBrush}"
                                      Foreground="{DynamicResource TextBrush}"
                                      BorderBrush="{DynamicResource InputBorderBrush}"
                                      SelectedItem="{Binding SortMode}"
                                      ItemsSource="{Binding SortModeOptions}" />
                            <Rectangle Width="1"
                                       Margin="0,2,12,2"
                                       Fill="{DynamicResource BorderBrush}" />
                            <Button Margin="0,0,6,0"
                                    Padding="10,3"
                                    Background="{DynamicResource WarnBrush}"
                                    Foreground="{DynamicResource WarnTextBrush}"
                                    BorderBrush="{DynamicResource WarnBorderBrush}"
                                    Content="{loc:Translate 'Universal Triggers Disable All'}"
                                    Command="{Binding DisableAllCommand}" />
                            <Button Padding="10,3"
                                    Background="{DynamicResource AccentDimBrush}"
                                    Foreground="{DynamicResource AccentBrush}"
                                    BorderBrush="{DynamicResource AccentBrush}"
                                    Content="{loc:Translate 'Universal Triggers Enable All'}"
                                    Command="{Binding EnableAllCommand}" />
                        </StackPanel>

                        <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                            <ToggleButton Style="{StaticResource FilterChipToggleStyle}"
                                          IsChecked="{Binding FilterMode, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=All}"
                                          Command="{Binding ShowAllCommand}">
                                <TextBlock Text="{Binding AllFilterText}" />
                            </ToggleButton>
                            <ToggleButton Style="{StaticResource FilterChipToggleStyle}"
                                          IsChecked="{Binding FilterMode, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=Active}"
                                          Command="{Binding ShowActiveCommand}">
                                <TextBlock Text="{Binding ActiveFilterText}" />
                            </ToggleButton>
                            <ToggleButton Style="{StaticResource FilterChipToggleStyle}"
                                          IsChecked="{Binding FilterMode, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=Disabled}"
                                          Command="{Binding ShowDisabledCommand}">
                                <TextBlock Text="{Binding DisabledFilterText}" />
                            </ToggleButton>
                            <ToggleButton Style="{StaticResource FilterChipToggleStyle}"
                                          IsChecked="{Binding FilterMode, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=NeedsFix}"
                                          Command="{Binding ShowNeedsFixCommand}">
                                <TextBlock Text="{Binding NeedsFixFilterText}" />
                            </ToggleButton>
                            <ToggleButton Style="{StaticResource FilterChipToggleStyle}"
                                          IsChecked="{Binding FilterMode, Converter={StaticResource EnumBooleanConverter}, ConverterParameter=FromFooma}"
                                          Command="{Binding ShowFoomaCommand}">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="pack://application:,,,/Assets/fooma-icon.png"
                                           Width="14" Height="14"
                                           Margin="0,0,5,0"
                                           VerticalAlignment="Center"
                                           RenderOptions.BitmapScalingMode="NearestNeighbor" />
                                    <TextBlock Text="{Binding FoomaFilterText}" VerticalAlignment="Center" />
                                </StackPanel>
                            </ToggleButton>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <ScrollViewer Grid.Row="2"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="14" x:Name="SectionStack">

                        <StackPanel Margin="0,0,0,14"
                                    Visibility="{Binding ChatSectionCount, Converter={StaticResource CountToVisibilityConverter}}">
                            <Border Background="{DynamicResource PanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="8,6"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="OnToggleChatSection">
                                <DockPanel LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" FontSize="14" Text="💬" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontWeight="Bold"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Text="{loc:Translate 'Universal Triggers Section Chat'}" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource MutedBrush}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Margin="6,0,0,0"
                                               Text="{Binding ChatSectionSuffix}" />
                                    <TextBlock DockPanel.Dock="Right"
                                               Foreground="{Binding IsChatSectionCollapsed, Converter={StaticResource CollapseChevronColorConverter}}"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Text="{Binding IsChatSectionCollapsed, Converter={StaticResource CollapseChevronTextConverter}}" />
                                    <Button DockPanel.Dock="Right"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Padding="6,2"
                                            Margin="0,0,8,0"
                                            Content="{loc:Translate 'Universal Triggers Section Disable All Mini'}"
                                            Command="{Binding DisableSectionCommand}"
                                            CommandParameter="Chat" />
                                </DockPanel>
                            </Border>
                            <ItemsControl ItemsSource="{Binding ChatSection}" Margin="4,8,4,0"
                                          Visibility="{Binding IsChatSectionCollapsed, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl Content="{Binding}"
                                                        ContentTemplate="{StaticResource UniversalTriggerCardTemplate}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,14"
                                    Visibility="{Binding RewardSectionCount, Converter={StaticResource CountToVisibilityConverter}}">
                            <Border Background="{DynamicResource PanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="8,6"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="OnToggleRewardSection">
                                <DockPanel LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" FontSize="14" Text="🎁" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontWeight="Bold"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Text="{loc:Translate 'Universal Triggers Section Reward'}" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource MutedBrush}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Margin="6,0,0,0"
                                               Text="{Binding RewardSectionSuffix}" />
                                    <TextBlock DockPanel.Dock="Right"
                                               Foreground="{Binding IsRewardSectionCollapsed, Converter={StaticResource CollapseChevronColorConverter}}"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Text="{Binding IsRewardSectionCollapsed, Converter={StaticResource CollapseChevronTextConverter}}" />
                                    <Button DockPanel.Dock="Right"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Padding="6,2"
                                            Margin="0,0,8,0"
                                            Content="{loc:Translate 'Universal Triggers Section Disable All Mini'}"
                                            Command="{Binding DisableSectionCommand}"
                                            CommandParameter="Reward" />
                                </DockPanel>
                            </Border>
                            <ItemsControl ItemsSource="{Binding RewardSection}" Margin="4,8,4,0"
                                          Visibility="{Binding IsRewardSectionCollapsed, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl Content="{Binding}"
                                                        ContentTemplate="{StaticResource UniversalTriggerCardTemplate}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,14"
                                    Visibility="{Binding BitsSectionCount, Converter={StaticResource CountToVisibilityConverter}}">
                            <Border Background="{DynamicResource PanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="8,6"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="OnToggleBitsSection">
                                <DockPanel LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" FontSize="14" Text="💎" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontWeight="Bold"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Text="{loc:Translate 'Universal Triggers Section Bits'}" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource MutedBrush}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Margin="6,0,0,0"
                                               Text="{Binding BitsSectionSuffix}" />
                                    <TextBlock DockPanel.Dock="Right"
                                               Foreground="{Binding IsBitsSectionCollapsed, Converter={StaticResource CollapseChevronColorConverter}}"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Text="{Binding IsBitsSectionCollapsed, Converter={StaticResource CollapseChevronTextConverter}}" />
                                    <Button DockPanel.Dock="Right"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Padding="6,2"
                                            Margin="0,0,8,0"
                                            Content="{loc:Translate 'Universal Triggers Section Disable All Mini'}"
                                            Command="{Binding DisableSectionCommand}"
                                            CommandParameter="Bits" />
                                </DockPanel>
                            </Border>
                            <ItemsControl ItemsSource="{Binding BitsSection}" Margin="4,8,4,0"
                                          Visibility="{Binding IsBitsSectionCollapsed, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl Content="{Binding}"
                                                        ContentTemplate="{StaticResource UniversalTriggerCardTemplate}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,14"
                                    Visibility="{Binding SubsSectionCount, Converter={StaticResource CountToVisibilityConverter}}">
                            <Border Background="{DynamicResource PanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="8,6"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="OnToggleSubsSection">
                                <DockPanel LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" FontSize="14" Text="⭐" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontWeight="Bold"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Text="{loc:Translate 'Universal Triggers Section Subs Combined'}" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource MutedBrush}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Margin="6,0,0,0"
                                               Text="{Binding SubsSectionSuffix}" />
                                    <TextBlock DockPanel.Dock="Right"
                                               Foreground="{Binding IsSubsSectionCollapsed, Converter={StaticResource CollapseChevronColorConverter}}"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Text="{Binding IsSubsSectionCollapsed, Converter={StaticResource CollapseChevronTextConverter}}" />
                                    <Button DockPanel.Dock="Right"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Padding="6,2"
                                            Margin="0,0,8,0"
                                            Content="{loc:Translate 'Universal Triggers Section Disable All Mini'}"
                                            Command="{Binding DisableSectionCommand}"
                                            CommandParameter="Subs" />
                                </DockPanel>
                            </Border>
                            <ItemsControl ItemsSource="{Binding SubsSection}" Margin="4,8,4,0"
                                          Visibility="{Binding IsSubsSectionCollapsed, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl Content="{Binding}"
                                                        ContentTemplate="{StaticResource UniversalTriggerCardTemplate}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,14"
                                    Visibility="{Binding FollowsSectionCount, Converter={StaticResource CountToVisibilityConverter}}">
                            <Border Background="{DynamicResource PanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="8,6"
                                    Cursor="Hand"
                                    MouseLeftButtonUp="OnToggleFollowsSection">
                                <DockPanel LastChildFill="False">
                                    <TextBlock DockPanel.Dock="Left" FontSize="14" Text="❤️" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontWeight="Bold"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Text="{loc:Translate 'Universal Triggers Section Follows'}" />
                                    <TextBlock DockPanel.Dock="Left"
                                               Foreground="{DynamicResource MutedBrush}"
                                               FontSize="11"
                                               VerticalAlignment="Center"
                                               Margin="6,0,0,0"
                                               Text="{Binding FollowsSectionSuffix}" />
                                    <TextBlock DockPanel.Dock="Right"
                                               Foreground="{Binding IsFollowsSectionCollapsed, Converter={StaticResource CollapseChevronColorConverter}}"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"
                                               Text="{Binding IsFollowsSectionCollapsed, Converter={StaticResource CollapseChevronTextConverter}}" />
                                    <Button DockPanel.Dock="Right"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Padding="6,2"
                                            Margin="0,0,8,0"
                                            Content="{loc:Translate 'Universal Triggers Section Disable All Mini'}"
                                            Command="{Binding DisableSectionCommand}"
                                            CommandParameter="Follows" />
                                </DockPanel>
                            </Border>
                            <ItemsControl ItemsSource="{Binding FollowsSection}" Margin="4,8,4,0"
                                          Visibility="{Binding IsFollowsSectionCollapsed, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl Content="{Binding}"
                                                        ContentTemplate="{StaticResource UniversalTriggerCardTemplate}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="3"
                        Background="{DynamicResource NestedPanelBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,1,0,0"
                        Padding="14,8">
                    <DockPanel LastChildFill="False">
                        <TextBlock DockPanel.Dock="Left"
                                   Foreground="{DynamicResource MutedBrush}"
                                   FontSize="11"
                                   VerticalAlignment="Center"
                                   Text="{loc:Translate 'Universal Triggers Footer Hint'}" />
                        <Button DockPanel.Dock="Right"
                                Background="Transparent"
                                Foreground="#FCA5A5"
                                BorderBrush="#663033"
                                Padding="10,4"
                                Content="{loc:Translate 'Universal Triggers Delete All'}"
                                Command="{Binding DeleteAllCommand}" />
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>

        <Grid Grid.Row="0" Grid.RowSpan="2"
              Visibility="{Binding IsEditorOpen, Converter={StaticResource BoolToVisibilityConverter}}">
            <Border Background="#80000000" MouseLeftButtonUp="OnEditorBackdropClicked" />
            <Border Width="480" HorizontalAlignment="Right"
                    Background="{DynamicResource PanelBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Background="{DynamicResource TitleBarBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="12,10">
                        <DockPanel LastChildFill="True">
                            <Button DockPanel.Dock="Right"
                                    Content="✕"
                                    Background="Transparent"
                                    Foreground="{DynamicResource TitleBarTextBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    Padding="6,2"
                                    Margin="6,0,0,0"
                                    Command="{Binding CloseEditorCommand}" />
                            <TextBlock Text="{Binding SelectedTrigger.Name}"
                                       Foreground="{DynamicResource TitleBarTextBrush}"
                                       FontWeight="Bold"
                                       FontSize="13"
                                       VerticalAlignment="Center" />
                        </DockPanel>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="14" x:Name="EditorStack">

                            <Border Background="{DynamicResource NestedPanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="12"
                                    Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Text="{loc:Translate 'Universal Triggers Editor Trigger Settings'}"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontSize="12"
                                               Margin="0,0,0,8" />
                                    <CheckBox Content="{loc:Translate 'Enabled'}"
                                              IsChecked="{Binding SelectedTrigger.IsEnabled, Mode=TwoWay}"
                                              Margin="0,0,0,8" />
                                    <UniformGrid Columns="2">
                                        <StackPanel Margin="0,0,8,0">
                                            <TextBlock Text="{loc:Translate 'Name'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <TextBox Text="{Binding SelectedTrigger.Name, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <StackPanel Margin="8,0,0,0">
                                            <TextBlock Text="{loc:Translate 'Trigger Type'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <ComboBox SelectedItem="{Binding SelectedTrigger.TriggerType, Mode=TwoWay}"
                                                      ItemsSource="{Binding DataContext.UniversalTriggerTypes, RelativeSource={RelativeSource AncestorType=Window}}" />
                                        </StackPanel>
                                    </UniformGrid>

                                    <StackPanel Margin="0,8,0,0">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SelectedTrigger.UsesChatCommand}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <UniformGrid Columns="2">
                                            <StackPanel Margin="0,0,8,0">
                                                <TextBlock Text="{loc:Translate 'Command'}"
                                                           Foreground="{DynamicResource MutedBrush}"
                                                           FontSize="10" />
                                                <TextBox Text="{Binding SelectedTrigger.CommandText, UpdateSourceTrigger=PropertyChanged}" />
                                            </StackPanel>
                                            <StackPanel Margin="8,0,0,0">
                                                <TextBlock Text="{loc:Translate 'Permission'}"
                                                           Foreground="{DynamicResource MutedBrush}"
                                                           FontSize="10" />
                                                <ComboBox SelectedItem="{Binding SelectedTrigger.ChatCommandPermission, Mode=TwoWay}"
                                                          ItemsSource="{Binding DataContext.ChatCommandPermissions, RelativeSource={RelativeSource AncestorType=Window}}" />
                                            </StackPanel>
                                        </UniformGrid>
                                    </StackPanel>

                                    <StackPanel Margin="0,8,0,0">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SelectedTrigger.UsesBits}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <UniformGrid Columns="2">
                                            <StackPanel Margin="0,0,8,0">
                                                <TextBlock Text="{loc:Translate 'Min bits'}"
                                                           Foreground="{DynamicResource MutedBrush}"
                                                           FontSize="10" />
                                                <TextBox Text="{Binding SelectedTrigger.MinimumBits, UpdateSourceTrigger=PropertyChanged}" />
                                            </StackPanel>
                                            <StackPanel Margin="8,0,0,0">
                                                <TextBlock Text="{loc:Translate 'Max bits'}"
                                                           Foreground="{DynamicResource MutedBrush}"
                                                           FontSize="10" />
                                                <TextBox Text="{Binding SelectedTrigger.MaximumBits, UpdateSourceTrigger=PropertyChanged}" />
                                            </StackPanel>
                                        </UniformGrid>
                                    </StackPanel>

                                    <StackPanel Margin="0,8,0,0">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SelectedTrigger.UsesSubscription}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                         <UniformGrid Columns="3">
                                             <StackPanel Margin="0,0,4,0">
                                                 <TextBlock Text="{loc:Translate 'Tier'}"
                                                            Foreground="{DynamicResource MutedBrush}"
                                                            FontSize="10" />
                                                 <ComboBox ItemsSource="{Binding SubscriptionTierOptions}"
                                                           DisplayMemberPath="Label"
                                                           SelectedValue="{Binding SelectedTrigger.SubscriptionTier, Mode=TwoWay}"
                                                           SelectedValuePath="Value" />
                                             </StackPanel>
                                            <StackPanel Margin="4,0">
                                                <TextBlock Text="{loc:Translate 'Min months'}"
                                                           Foreground="{DynamicResource MutedBrush}"
                                                           FontSize="10" />
                                                <TextBox Text="{Binding SelectedTrigger.MinimumMonths, UpdateSourceTrigger=PropertyChanged}" />
                                            </StackPanel>
                                            <StackPanel Margin="4,0,0,0">
                                                <TextBlock Text="{loc:Translate 'Max months'}"
                                                           Foreground="{DynamicResource MutedBrush}"
                                                           FontSize="10" />
                                                <TextBox Text="{Binding SelectedTrigger.MaximumMonths, UpdateSourceTrigger=PropertyChanged}" />
                                            </StackPanel>
                                        </UniformGrid>
                                    </StackPanel>

                                    <UniformGrid Columns="3" Margin="0,8,0,0">
                                        <StackPanel Margin="0,0,4,0">
                                            <TextBlock Text="{loc:Translate 'Global Delay (s)'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <TextBox Text="{Binding SelectedTrigger.GlobalDelaySeconds, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <StackPanel Margin="4,0">
                                            <TextBlock Text="{loc:Translate 'User Delay (s)'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <TextBox Text="{Binding SelectedTrigger.UserDelaySeconds, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <CheckBox Content="{loc:Translate 'Run random action'}"
                                                  IsChecked="{Binding SelectedTrigger.ExecuteRandomAction, Mode=TwoWay}"
                                                  VerticalAlignment="Bottom"
                                                  Margin="4,0,0,0" />
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource NestedPanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="12"
                                    Margin="0,0,0,14">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedTrigger.UsesChannelPointReward}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{loc:Translate 'Universal Triggers Editor Twitch Reward'}"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontSize="12"
                                               Margin="0,0,0,8" />
                                    <TextBlock Text="{loc:Translate 'Sync Mode'}"
                                               Foreground="{DynamicResource MutedBrush}"
                                               FontSize="10" />
                                    <ComboBox ItemsSource="{Binding RewardSyncModeOptions}"
                                              DisplayMemberPath="Label"
                                              SelectedValue="{Binding SelectedTrigger.RewardSyncMode, Mode=TwoWay}"
                                              SelectedValuePath="Value"
                                              Margin="0,0,0,8" />
                                    <StackPanel Margin="0,0,0,8">
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SelectedTrigger.UsesCreateOrManageReward}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text="{loc:Translate 'Reward Title'}"
                                                   Foreground="{DynamicResource MutedBrush}"
                                                   FontSize="10" />
                                        <TextBox Text="{Binding SelectedTrigger.RewardTitle, UpdateSourceTrigger=PropertyChanged}" />
                                    </StackPanel>
                                    <UniformGrid Columns="2">
                                        <StackPanel Margin="0,0,8,0">
                                            <TextBlock Text="{loc:Translate 'Cost'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <TextBox Text="{Binding SelectedTrigger.RewardCost, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <StackPanel Margin="8,0,0,0">
                                            <TextBlock Text="{loc:Translate 'Cooldown (s)'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <TextBox Text="{Binding SelectedTrigger.RewardCooldownSeconds, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                    </UniformGrid>
                                    <UniformGrid Columns="2" Margin="0,8,0,0">
                                        <StackPanel Margin="0,0,8,0">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SelectedTrigger.UsesCreateOrManageReward}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="{loc:Translate 'Ready Color'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <TextBox Text="{Binding SelectedTrigger.ManagedRewardReadyColor, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <StackPanel Margin="8,0,0,0">
                                            <StackPanel.Style>
                                                <Style TargetType="StackPanel">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SelectedTrigger.UsesCreateOrManageReward}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </StackPanel.Style>
                                            <TextBlock Text="{loc:Translate 'Cooldown Color'}"
                                                       Foreground="{DynamicResource MutedBrush}"
                                                       FontSize="10" />
                                            <TextBox Text="{Binding SelectedTrigger.ManagedRewardCooldownColor, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                    </UniformGrid>
                                    <CheckBox Content="{loc:Translate 'Delete reward when inactive'}"
                                              IsChecked="{Binding SelectedTrigger.DeleteManagedRewardWhenInactive, Mode=TwoWay}"
                                              Margin="0,8,0,0">
                                        <CheckBox.Style>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SelectedTrigger.UsesCreateOrManageReward}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </CheckBox.Style>
                                    </CheckBox>
                                    <TextBlock Foreground="{DynamicResource MutedBrush}"
                                               FontSize="10"
                                               TextWrapping="Wrap"
                                               Margin="0,8,0,0"
                                               Text="{Binding RewardVisibilityStatusText}" />
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource NestedPanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="12"
                                    Margin="0,0,0,14">
                                <StackPanel>
                                    <TextBlock Text="{loc:Translate 'Universal Triggers Editor Avatar Readiness'}"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource TextBrush}"
                                               FontSize="12"
                                               Margin="0,0,0,8" />
                                    <ItemsControl ItemsSource="{Binding AvatarReadinessRows}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <DockPanel LastChildFill="True" Margin="0,2">
                                                    <TextBlock DockPanel.Dock="Right"
                                                               Foreground="{Binding StatusBrush}"
                                                               FontSize="10"
                                                               Text="{Binding StatusText}" />
                                                    <TextBlock Foreground="{DynamicResource TextBrush}"
                                                               FontSize="11"
                                                               Text="{Binding Address}"
                                                               TextTrimming="CharacterEllipsis" />
                                                </DockPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    <TextBlock Foreground="{DynamicResource MutedBrush}"
                                               FontSize="10"
                                               FontStyle="Italic"
                                               Text="{loc:Translate 'Universal Triggers Editor No Avatar Params'}"
                                               Visibility="{Binding HasNoAvatarParams, Converter={StaticResource BoolToVisibilityConverter}}" />
                                </StackPanel>
                            </Border>

                            <Border Background="{DynamicResource NestedPanelBrush}"
                                    BorderBrush="{DynamicResource InputBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="12"
                                    Padding="12">
                                <StackPanel>
                                    <DockPanel LastChildFill="False" Margin="0,0,0,8">
                                        <TextBlock DockPanel.Dock="Left"
                                                   Text="{loc:Translate 'Universal Triggers Editor OSC Actions'}"
                                                   FontWeight="Bold"
                                                   Foreground="{DynamicResource TextBrush}"
                                                   FontSize="12" />
                                        <Button DockPanel.Dock="Right"
                                                Style="{StaticResource SecondaryButtonStyle}"
                                                Padding="6,2"
                                                Content="{loc:Translate 'Universal Triggers Editor Add Action'}"
                                                Command="{Binding AddActionCommand}" />
                                    </DockPanel>
                                    <ItemsControl ItemsSource="{Binding SelectedTrigger.Actions}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{DynamicResource InputBrush}"
                                                        BorderBrush="{DynamicResource InputBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="6"
                                                        Padding="6"
                                                        Margin="0,4">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <DockPanel Grid.Row="0" LastChildFill="True">
                                                            <Button DockPanel.Dock="Right"
                                                                    Style="{StaticResource SecondaryButtonStyle}"
                                                                    Padding="4,1"
                                                                    Margin="6,0,0,0"
                                                                    Content="🗑"
                                                                    Command="{Binding DataContext.RemoveActionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                    CommandParameter="{Binding}" />
                                                            <TextBox Text="{Binding OscAddress, UpdateSourceTrigger=PropertyChanged}" />
                                                        </DockPanel>
                                                        <Grid Grid.Row="1" Margin="0,4,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="140" />
                                                                <ColumnDefinition Width="*" MinWidth="80" />
                                                                <ColumnDefinition Width="*" MinWidth="120" />
                                                            </Grid.ColumnDefinitions>
                                                            <ComboBox Grid.Column="0"
                                                                      SelectedItem="{Binding ValueKind, Mode=TwoWay}"
                                                                      ItemsSource="{Binding DataContext.UniversalTriggerValueKinds, RelativeSource={RelativeSource AncestorType=Window}}"
                                                                      Margin="0,0,4,0" />
                                                            <Grid Grid.Column="1">
                                                                <CheckBox VerticalAlignment="Center"
                                                                          Margin="4,0"
                                                                          Content="{loc:Translate 'Universal Triggers Value On'}"
                                                                          IsChecked="{Binding TargetValue, Converter={StaticResource UniversalTriggerBoolConverter}, Mode=TwoWay}"
                                                                          Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Bool}" />
                                                                <TextBox Margin="2,0"
                                                                         Text="{Binding TargetValue, Converter={StaticResource UniversalTriggerIntConverter}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Int}" />
                                                                <TextBox Margin="2,0"
                                                                         Text="{Binding TargetValue, Converter={StaticResource UniversalTriggerFloatConverter}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Float}" />
                                                                <TextBox Margin="2,0"
                                                                         Text="{Binding TargetValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=String}" />
                                                            </Grid>
                                                            <Grid Grid.Column="2">
                                                                <CheckBox VerticalAlignment="Center"
                                                                          Margin="4,0"
                                                                          Content="{loc:Translate 'Universal Triggers Value On'}"
                                                                          IsChecked="{Binding DefaultValue, Converter={StaticResource UniversalTriggerBoolConverter}, Mode=TwoWay}"
                                                                          Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Bool}" />
                                                                <TextBox Margin="4,0,0,0"
                                                                         Text="{Binding DefaultValue, Converter={StaticResource UniversalTriggerIntConverter}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Int}" />
                                                                <TextBox Margin="4,0,0,0"
                                                                         Text="{Binding DefaultValue, Converter={StaticResource UniversalTriggerFloatConverter}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Float}" />
                                                                <TextBox Margin="4,0,0,0"
                                                                         Text="{Binding DefaultValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                                         Visibility="{Binding ValueKind, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=String}" />
                                                            </Grid>
                                                        </Grid>
                                                        <DockPanel Grid.Row="2" LastChildFill="True" Margin="0,4,0,0">
                                                            <CheckBox DockPanel.Dock="Right"
                                                                      Content="{loc:Translate 'Queue'}"
                                                                      IsChecked="{Binding AddToQueue, Mode=TwoWay}" />
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="{loc:Translate 'Duration (s):'}"
                                                                           Foreground="{DynamicResource MutedBrush}"
                                                                           FontSize="10"
                                                                           VerticalAlignment="Center"
                                                                           Margin="0,0,4,0" />
                                                                <TextBox Text="{Binding DurationSeconds, UpdateSourceTrigger=PropertyChanged}"
                                                                         Width="60" />
                                                            </StackPanel>
                                                        </DockPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                        </StackPanel>
                    </ScrollViewer>

                    <Border Grid.Row="2"
                            Background="{DynamicResource TitleBarBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,1,0,0"
                            Padding="12,8">
                        <DockPanel LastChildFill="False">
                            <Button DockPanel.Dock="Left"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="10,4"
                                    Foreground="#FCA5A5"
                                    BorderBrush="#663033"
                                    Content="{loc:Translate 'Universal Triggers Editor Delete'}"
                                    Command="{Binding DeleteSelectedTriggerCommand}" />
                            <Button DockPanel.Dock="Right"
                                    Background="{DynamicResource AccentBrush}"
                                    Foreground="{DynamicResource ComboTextBrush}"
                                    Padding="12,4"
                                    FontWeight="Bold"
                                    Content="{loc:Translate 'Universal Triggers Editor Save'}"
                                    Command="{Binding SaveEditorCommand}" />
                            <Button DockPanel.Dock="Right"
                                    Margin="0,0,8,0"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Padding="10,4"
                                    Content="{loc:Translate 'Universal Triggers Editor Test Now'}"
                                    Command="{Binding TestSelectedTriggerCommand}" />
                        </DockPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
    </Border>
</Window>
