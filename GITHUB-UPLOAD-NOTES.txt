Crystal Relay GitHub staging copy

Folder purpose:
- This folder is a clean staging copy for GitHub upload.
- Repo metadata, local caches, backups, releases, test builds, and build outputs were excluded.

Current status:
- Safe for a private GitHub repo review.
- Not yet safe for a public GitHub repo without one more cleanup pass.

Public upload blocker:
- The desktop app still contains the About-page fallback relay wiring in:
  - VrcTwitchOscBridge\Services\TwitchApiClient.cs
  - VrcTwitchOscBridge\ViewModels\MainWindowViewModel.cs
- That code references a personal Cloudflare Worker fallback path and related request header wiring.

Recommended next step before public upload:
- Remove the personal About fallback relay from the app, or replace it with a public-safe configuration model.

Suggested upload flow:
1. Create a private GitHub repo first.
2. Upload this staging folder.
3. After the About fallback relay is removed, publish the cleaned repo as public.
